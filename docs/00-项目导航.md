---
title: 项目导航
tags: [导航, MOC, 教务系统]
updated: 2026-09-21
---

# 项目导航（唯一入口）

> 这是整个项目的 **Map of Content**。所有文档从这里进入。
> **找不到答案时先回这里，不要新建文档。**

---

## 一、三条铁律（防止文档腐化）

1. **单一事实源**：同一件事只在一处定义。要引用就**双链** `[[文件名]]`，**绝不复制粘贴**。复制 = 双份 = 必然过期。
2. **会变的 与 不变的 分开**：计划、进度会变，可以改；**决策理由不变，只追加**。
3. **不知道写哪就问**：宁可先记在 `.workbuddy/memory/YYYY-MM-DD.md` 日志里，也不要顺手新建一份"XX说明.md"。

---

## 二、权威范围表（你想知道什么 → 查哪里）

| 你想知道… | **唯一权威** | 不要去查 |
| --- | --- | --- |
| 系统能做什么（对外说明） | [[README]] | PROGRESS / ROADMAP |
| 现在做到哪了（已完成的） | [[PROGRESS]]（位于 docs/） | ROADMAP |
| 接下来做什么（待做的计划） | [[ROADMAP]]（位于 docs/） | PROGRESS |
| **为什么这么定（决策理由）** | `docs/decisions/` | ROADMAP（只有结论，没有理由） |
| 数据库长什么样 | `server/database.md` | 迁移脚本（只看增量变更） |
| 接口怎么调 | `docs/api.md` | 后端源码 |
| 接口的机器可读定义 | `docs/openapi.yaml` | — |
| 开发时的硬规则 / 红线 | [[WORKBUDDY]]（项目根目录） | — |
| 代码在哪（目录结构与定位索引） | docs/项目地图.md | 全项目扫描 |
| 本机环境坑 / 开发踩坑 | [[WORKBUDDY]] §八 | PROGRESS（已去重，2026-09-12） |
| 这次会话干了什么 | `.workbuddy/memory/YYYY-MM-DD.md` | — |
| 项目长期不变的事实 | `.workbuddy/memory/MEMORY.md` | — |

> **最常见的错误**：在 PROGRESS 里写计划、在 ROADMAP 里写进度。这两份文件一旦混淆，进度就永远对不上。

---

## 三、文档地图

### 顶层
- [[README]] — 对外说明：功能特性、技术栈、部署、默认账号、API 一览
- [[WORKBUDDY]] — **AI 开发的最高优先级规则**（pure-admin 规范、迁移规则、测试要求）
- [[PROGRESS]] — **已完成**的事实 + 已知问题 + 浏览器验证记录
- [[ROADMAP]] — **待做**的计划：决策基线、Non-goals、RICE 打分、13 周排期、风险登记

### 设计决策（ADR）
目录：`docs/decisions/` — **一个决策一个文件，只追加，不修改**

| ADR | 决策 | 状态 |
| --- | --- | --- |
| [[ADR-001-单校区自用不做多校区架构]] | 单校区自用，不为多校区预留 | 已接受 |
| [[ADR-002-家校不做家长端与出网触达]] | 不做家长端、不做任何出网触达 | 已接受 |
| [[ADR-003-财务只做学员账户与剩余学费口径]] | 财务只做学员账户 + 剩余学费口径 | 已接受，**口径待校长确认** |
| [[ADR-004-AI学情分析采用本地指标加大模型润色]] | AI = 本地指标 + 大模型润色，禁止 text2sql | 已接受 |
| [[ADR-005-课堂评价采用全班默认良好只标异常]] | 课评按「只标异常」设计 | 已接受 |
| [[ADR-006-AI学情报告的数据模型与录入粒度]] | AI 学情报告的数据模型与录入粒度 | **提案（待业务方确认）** |
| [[ADR-007-AI能力采用独立工作台加只读网关与最小权限凭证]] | AI 独立工作台 + 只读网关 + 最小权限凭证 | 已接受 |
| [[ADR-008-数据资产化与AI产出层架构]] ★ | **系统为唯一数据源/唯一仓库；数据库存索引、文件存磁盘；Dify 是加工厂（只读）不是仓库；产出层分期 xlsx→PDF→PPT→视频暂缓** | 已决定（2026-09-21） |

**什么时候该写 ADR**：任何**三个月后会被重新质疑**的决定。典型信号——"我们为什么不做 X？""当初为什么选 A 不选 B？"

### 技术文档
- `docs/api.md` — API 权威文档（人类可读）
- `docs/openapi.yaml` — OpenAPI 3.0 机器可读规范
- `server/database.md` — 数据库 schema 权威清单 + 版本历史（当前 v15）
- `server/src/migrations/` — 迁移脚本 `0NN-*.js`，**禁止修改历史迁移**

### 评审报告（按时点归档，只读存档，不再修改）
| 报告 | 日期 | 内容 |
| --- | --- | --- |
| `docs/上线评估报告-2026-09-12.md` | 2026-09-12 | 上线门禁 Gate 1/2 评估（综合 71/100） |
| `docs/业务流程与权限梳理报告-2026-09-12.md` | 2026-09-12 | 学生全链路流程 + 权限点核对（只读） |
| `docs/全面测试报告-2026-09-12.md` | 2026-09-12 | 全角色全流程测试结果 |
| `docs/过度设计审计报告-2026-09-14.md` | 2026-09-14 | ponytail-audit 过度设计审计 + 执行结果（净减约 1,000 行 / 5 依赖） |

### 记忆与日志
- `.workbuddy/memory/MEMORY.md` — 项目长期事实（**每轮自动注入 AI 上下文，3000 字符上限，保持精简**）
- `.workbuddy/memory/YYYY-MM-DD.md` — 每日工作日志（append-only）

---

## 四、当前状态速览

> 本段为**快照**，最后更新 **2026-09-21**。数据以仓库内脚本实测为准，不要凭记忆填写。

- 数据库版本：**v18**（v16 使用反馈 / v17 AI 配置中心 / v18 学生成长时间轴；权威清单见 `server/database.md`）
- 回归基线（**2026-09-20 实测**，均为仓库内可复现脚本）：

  | 套件 | 结果 | 命令 |
  | --- | --- | --- |
  | 全页面冷启动巡检 | **28/28** | `python _verify_test/verify-all-pages.py http://localhost:8080 http://127.0.0.1:3000` |
  | e2e 生命周期 | **80/80** | `BASE=http://127.0.0.1:3000 node server/scripts/e2e-lifecycle.mjs` |
  | analytics 冒烟 | **22/22** | `BASE=http://127.0.0.1:3000 node server/scripts/analytics-smoke.mjs` |
  | 浏览器 P0 | **31/31** | `python server/scripts/ui-p0-verify.py http://localhost:8080 http://127.0.0.1:3000` |
  | Docker 部署验证 | **9/9** | `bash server/scripts/docker-verify.sh` |
  | 业务链闭环探针 | **49/49** | `BASE=http://127.0.0.1:3000 node _verify_test/probe-business-chain.mjs` |

- 源码规模：前端 156 文件（30 个页面），后端 60 文件（2026-09-20 统计）
- 上线状态：**已具备部署条件**（2026-09-20 完成教务系统全闭环验证）；当前处于 Phase 0（只做稳定，不开发新功能）
- 进度优先级（**2026-09-20 用户拍板**）：**教务系统闭环优先于一切新功能**；AI 工作台为下一步计划，需先收集校区老师反馈再开工
- 路线图：Phase 0 → 5（13 周 / 约 66 人日）+ **Phase 6 数据资产化与产出层（2026-09-21 新增方向，未排期）**，见 [[ROADMAP]]
- ★ **数据方向（2026-09-21 拍板）**：**系统是唯一数据源、也是唯一仓库**；**Dify 是加工厂（只读）不是仓库**，AI 产出必须落回自有库（索引入库 + 文件落盘 `server/data/assets/`）。详见 [[ADR-008-数据资产化与AI产出层架构]]

---

## 五、路线图阶段速查

| 阶段 | 周次 | 内容 |
| --- | --- | --- |
| Phase 0 | 上线前 | 上线保障：浏览器验证补齐、git 初始化、备份恢复演练、培训 |
| Phase 1 | 1–2 | 数据地基：学员账户 + 课堂评价 + 知识点体系 |
| Phase 2 | 3–4 | 排课治理：停课日历 + 代课 + 教室冲突 + 批量排课 |
| Phase 3 | 5–6 | 招生漏斗：试听预约 + 线索公海分配与跟进提醒 |
| Phase 4 | 7–9 | 学情分析：LLM provider + 指标引擎 + 预警 + AI 报告 + 班级概览 |
| Phase 5 | 10–12 | 深化治理：校长问数 + 组件迁移 + 移动端响应式 + 报表增强 |
| Phase 6 | 未排期 | **数据资产化与产出层**：文件资产表 + `assets/` 目录 → 数据表 → PDF → PPT；Dify 知识库启用（见 [[ADR-008-数据资产化与AI产出层架构]]） |

---

## 六、用 Obsidian 打开本项目（可选，但强烈推荐）

**做法**：Obsidian →「打开本地仓库」→ 选本项目根目录。**不要复制文件到别的 vault**——复制就是双份，必然过期。

**必配（不做会卡死）**：设置 → 文件与链接 →「已忽略的文件」，加上这些：

```
node_modules/
server/node_modules/
dist/
evidence/
_verify_test/
.git/
server/data/
pnpm-lock.yaml
package-lock.json
```
> 本机 AI 工具的忽略规则**统一由 `.aiignore` 一份承担**（2026-09-21 清理：原先 `.cursorignore` / `.copilotignore` / `.ignore` 三份同内容文件已删除，见 WORKBUDDY.md §十）

> 不配的话 Obsidian 会扫描 `node_modules/` 的几十万个文件：启动卡死、关系图谱被依赖包污染成蜘蛛网。

**建议插件（只推真正必要的，别一上来装一堆）**：
- `Dataview`（可选）：把本导航页的 ADR 表格变成自动生成的动态索引
- 核心插件「模板」：给 ADR 建个模板，避免格式漂移

**建议设置**：设置 → 编辑器 →「严格换行」**关掉**（中文写作友好）。

**注意**：`.obsidian/` 是工作区配置，不要提交进 git（加到 `.gitignore`）。

---

## 七、明确不做（Non-goals）

详见 [[ROADMAP]] §二。任何一项要启用，必须回到 `docs/ROADMAP.md` §〇 修改对应决策并重新排期。

多校区 / 家长端 / 出网触达 / 提成绩效 / 发票优惠线上支付 / 完整题库 / 作业 / 考勤硬件 / 移动端 App / 进销存 / 自定义字段与角色

> ⚠️ **2026-09-21 变更**：「**RAG 知识库**」**已从 Non-goals 解除**，纳入规划（由 **Dify** 承载，不自建向量库）。其余项全部保持不变。见 [[ADR-008-数据资产化与AI产出层架构]]
