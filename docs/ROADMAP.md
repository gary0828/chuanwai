# 产品路线图（ROADMAP）

> 生成日期：**2026-09-12** ｜ 依据：深度需求访谈（12 个决策点全部收敛）
> 适用范围：**单校区自用**培训机构员工端 CRM ｜ 规模：**在校学员 200–500 / 员工 5–15**
> 本文件与 `PROGRESS.md` 分工：`PROGRESS.md` 记录**已完成的事实**，本文件记录**待做的计划**。每阶段完成后把结论回填 `PROGRESS.md`。

---

## 〇、决策基线（已拍板，改动需重新评估下游）

| # | 决策项 | 结论 | 下游影响 |
| --- | --- | --- | --- |
| D1 | 服务范围 | **单校区自用** | 砍掉多校区、租户隔离、跨校区汇总、转校；不为这些预留架构 |
| D2 | 家校边界 | **不做家长端，不做任何出网触达** | 通知模块降级为「内部留痕」；短信/微信模板/推送全部不做 |
| D3 | 优先级方法 | **痛点驱动 + RICE 打分** | 允许主动放弃竞品标配（见 §二 Non-goals） |
| D4 | AI 实现方式 | **混合：本地算结构化指标 + DeepSeek 写自然语言** | 数字不出本地逻辑，杜绝幻觉；只把脱敏指标送模型 |
| D5 | 网络与模型 | **内网可出公网，调 DeepSeek 官方 API**，数据本地保存 | 必须做字段白名单脱敏；未配置 Key 时自动降级规则引擎版 |
| D6 | AI 触发方式 | **应用内一键生成**，结果落库可回看 / 可打印 | 不需要任务调度；生成记录要能审计（谁、何时、用了哪个模型） |
| D7 | 知识库 / 题库 | **本期只做最小依赖：知识点体系**（学科→知识点树 + 成绩知识点标签）<br>⚠️ **2026-09-21 部分修订**：RAG 知识库**纳入规划**（由 Dify 承载）→ 见 [[ADR-008-数据资产化与AI产出层架构]] | 不做组卷、在线答题、阅卷、错题本；~~RAG 知识库~~（已解除） |
| D8 | 财务边界 | **只做「学员账户」**：交了多少学费、扣了多少课时、剩余多少学费 | 不做提成、发票、优惠折扣、日结、分期、线上支付 |
| D9 | 排课边界 | **内部全套**：教室冲突 + 代课 + 批量排课 + 停课日历 | 不触达家长；「家长不知情」痛点本期不解决（已知取舍） |
| D10 | 招生边界 | **试听预约 + 线索公海分配 + 跟进提醒** | 不含转介绍返利、活动管理、渠道 ROI |
| D11 | 投入节奏 | **全职推进，按周冲刺** | 每阶段结束必须验收，不跨阶段并行 |
| D12 | 上线时间 | ~~下周 go-live~~ → **2026-09-20 教务系统全闭环验证通过，已具备部署条件**（28/28 · 80/80 · 22/22 · 31/31 · 9/9 · 49/49）；Phase 0 只做稳定与数据准备，**不开发新功能** | 新功能从 Phase 1 起，go-live 后启动 |
| D13 | AI 落地方式（2026-09-14 追加） | **独立工作台 + 只读网关 + 最小权限凭证**：`ai-workbench/` 独立部署（:8082），登录态由教务系统一次性票据统一提供，取数只走 `/api/agent/*`；底座可插拔（服务端模型推荐 / 规则引擎零配置降级） | 详见 `docs/decisions/ADR-007-AI能力采用独立工作台加只读网关与最小权限凭证.md` 与 `docs/AI教师助手调研分析报告-2026-09-14.md`；本期已交付教学全流程工作台、使用反馈模块（v16）、配置中心（v17）、**C1 密钥服务端代持收口（2026-09-20）**；课评 / 知识点 / 题库仍待建 |
| D14 | 推进顺序（**2026-09-20 用户拍板**） | **教务系统闭环优先于一切新功能**：教务系统跑通流畅才能为 AI 工作台提供真实数据作参考。AI 工作台是下一步计划（耗时大），**开工前须先收集校区老师的真实使用意见** | 教务系统的「逻辑不顺畅 / 数据不通 / 关联做不好」优先修；AI 工作台新菜单待老师反馈后再逐个推进（按 2026-09-14 约定的「逐个菜单推进 + 三类来源分类」流程） |
| D15 | 数据资产化与产出层方向（**2026-09-21 用户拍板**） | ★★ **系统是唯一数据源、也是唯一仓库**：数据只能在系统内产生与沉淀；**Dify 是加工厂（只读）不是仓库**，AI 产出必须落回自有数据库。<br>**数据库只存索引（元数据），文件本体存磁盘** `server/data/assets/`（按 学员 / 班级 / 教师 分目录，随数据卷备份）。<br>**知识库三层分开**：业务数据→系统 SQLite；参考资料→Dify 知识库（只读）；AI 产出→系统（索引入库 + 文件落盘）。<br>**产出层分期**：xlsx（已有）→ PDF → PPT → 视频（暂缓）。<br>**MCP 仍不引入**；Obsidian 仅作人的资料工作台、不作为 AI 知识库 | 详见 `docs/decisions/ADR-008-数据资产化与AI产出层架构.md`。需新增「文件资产」表（只存元数据）+ 建立 `assets/` 目录；`dify/` 由「暂不需要」转为**规划启用**（:8081）；磁盘容量需评估（文本 32GB 够用，存视频需 100GB+）；**采集铁律不变——永不强制、嵌入既有流程** |

---

## 一、现状判断

**强项（护城河，不要动）**：课时包联动扣减与回补、缺勤→通知→撤销的幂等链路、课消收入确认分摊、调课审批自动同步课表、线索一键转化（建档+报班事务）、成长档案全生命周期时间线。这套**业务联动完整度**超过多数收费 SaaS。

**整体覆盖度约 53%**：强在「教务执行闭环」，弱在「触达 / 经营 / 移动化」。

**最大结构性缺口**：系统在库里**没有「学生日常表现」数据**。现有可分析数据只有考勤状态、考试成绩、课时消耗、缴费记录——AI 学情分析若无课评数据，只能基于三次考试做线性回归，产出正确的废话。**因此「课堂评价」被 AI 需求倒逼为前置模块。**

---

## 二、Non-goals（明确不做，防止范围蔓延）

> 以下为**本期明确排除项**。任何一项要启用，必须回到 §〇 修改对应决策并重新排期。

**架构层面**
- 多校区 / 租户隔离 / 跨校区报表 / 学员转校
- SaaS 化、对外销售、计费与订阅
- 自定义字段、自定义角色权限（admin / teacher 两角色够用）

**家校层面**
- 家长端（小程序 / H5 / 公众号）
- 家长账号体系与家校绑定关系（v14 已删除，不回摆）
- 短信 / 微信模板消息 / App 推送等任何出网触达通道
- 家长回执、家校互评、在线投诉

**财务层面**
- 员工提成、业绩归属、课时工资、绩效工资
- 收据 / 发票打印、电子发票
- 优惠审批、优惠券、团报折扣、分期收款
- 线上支付（微信 / 支付宝 / 聚合支付）
- 日结对账单、会计凭证、总账

**教学层面**
- 完整题库（组卷、在线答题、自动阅卷、错题本）
- ~~RAG 知识库（文档解析、向量库、embedding 检索）~~ ← **2026-09-21 解除**：RAG 知识库**纳入规划**，由 **Dify** 承载（不自建向量库）。见 [[ADR-008-数据资产化与AI产出层架构]] §4
- 作业布置与提交批改
- 直播课 / 双师课堂 / 录播

> **2026-09-14 补充**：AI 教学工作台（`ai-workbench/`）已交付教学流程的「壳」—— 课程设计 / 备课 / 授课流程 / 作业设计 / 作业检查与评价 / 家长反馈 / 学情分析与报告 / 知识库。但**上述 Non-goals 依然有效**：工作台对真实库尚无的表（课评、知识点、题库）统一标注「待建设」并继续展示演示内容，不构成本期交付承诺。若要把题库纳入排期，需先回到 §〇 修订 D7 并重新评估工期。

> **2026-09-21 修订（见 [[ADR-008-数据资产化与AI产出层架构]]）**：上文「RAG 知识库」一项**已解除排除**，正式纳入规划，由 Dify 承载（**不自建向量库 / embedding 管线**）。**其余 Non-goals 全部保持不变**，其中以下四条为**强化重申**：
> - ❌ **不做**自建 RAG 基础设施（向量库、embedding pipeline）→ 交 Dify
> - ❌ **不把** AI 产出留在 Dify → **必须落回系统数据库 + 磁盘**
> - ❌ **不做**视频生成（本期暂缓，性价比低、效果不可控）
> - ❌ **不改**「纯员工端 CRM」定位：学生 / 家长仍无账号；AI 产出为**内部资料**，不自动触达家长

**其他**
- 考勤硬件（刷卡机、人脸机、二维码签到大屏）
- 移动端 App / 小程序（教师端）
- 进销存（教材教具）、加盟商管理

---

## 三、优先级方法

采用 **RICE = Reach × Impact × Confidence ÷ Effort**

- **Reach**：月均关键动作次数（系统自动执行或人工操作的次数，估算值，需业务校准）
- **Impact**：3 = 巨大（影响收入或流失）/ 2 = 高 / 1 = 中 / 0.5 = 低
- **Confidence**：对 Reach 与 Impact 估计的把握（0.5 / 0.8 / 0.9）
- **Effort**：人日（含实现 + 联调 + 浏览器验证 + 文档）

**排序例外**：标注为「前置依赖」的项不参与 RICE 排序，按依赖顺序强制前置（RICE 会系统性低估地基类功能）。

---

## 四、功能池与打分

| ID | 功能 | R（次/月） | I | C | E（人日） | RICE | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| F2 | **课堂评价（课评）** | 960 | 3 | 0.8 | 3.5 | **658** | AI 燃料 + 家长最在乎的信息；必须做「批量 + 只标异常」 |
| F12 | **学员学情报告（AI 生成）** | 500 | 3 | 0.7 | 3 | **350** | 依赖 F10 / F3 / F15 |
| F9 | **线索公海 + 分配 + 跟进提醒 + 超时回收** | 600 | 2 | 0.8 | 5 | **192** | 直接治「线索跟丢」 |
| F1 | **学员账户（实缴 / 已扣课时 / 剩余学费）** | 200 | 3 | 0.9 | 4 | **135** | 含全校「剩余学费总额（预收账款）」指标 |
| F13 | 班级学情概览 | 80 | 2 | 0.8 | 2 | **64** | 依赖 F10 |
| F11 | 预警推送（主动找人） | 30 | 3 | 0.9 | 2 | **40.5** | 依赖 F10；RICE 低估其战略价值，人工提级 |
| F8 | 试听 / 体验课预约 + 反馈 + 转化率 | 40 | 3 | 0.8 | 5 | **19.2** | 教培转化核心环节，当前完全缺失 |
| F6 | 教室 / 场地资源与冲突检测 | 40 | 2 | 0.8 | 3 | **21.3** | 依赖 D9 |
| F4 | 代课 / 换老师 | 20 | 2 | 0.9 | 2 | **18** | 依赖 D9 |
| F5 | 节假日停课日历 + 批量停课 | 8 | 2 | 0.9 | 2 | **7.2** | 依赖 D9；频次低但每次影响面广 |
| F14 | 校长问数（预置指标语义版） | 20 | 2 | 0.5 | 3 | **6.7** | **降级方案**：不做 text2sql，只允许命中已有 12 个指标 |
| F7 | 整学期批量排课 | 2 | 2 | 0.9 | 2 | **1.8** | 每学期仅用 1–2 次，但单次省数小时 |
| F3 | 知识点体系最小集 | — | — | — | 2.5 | **前置依赖** | F12 / F13 的燃料，越早建越早积累数据 |
| F10 | 本地结构化指标引擎 | — | — | — | 4 | **前置依赖** | 出勤率 / 成绩趋势 / 知识点掌握度 / 离群检测 |
| F15 | LLM provider 抽象 + 脱敏 + 降级 | — | — | — | 2 | **前置依赖** | 可配置 base_url / model / key；未配置则走规则引擎版 |

### 关于「校长自然语言问数」的风险提示

D6 选择了「校长经营洞察」，但**通用 text2sql 会让大模型写错 SQL 或理解错口径**，产出一个看起来专业、实际错误的数字——校长拿它做决策会出事。因此本期**降级为「预置指标语义问答」**：把用户输入映射到 `/api/analytics/metrics` 已定义的 12 个指标（语义匹配 + 槽位填充），**不允许生成任意 SQL**，命中不了就明确回答「这个问题我答不了」并列出可问清单。

---

## 五、路线图（按周）

### Phase 0 · Go-live 保障（约 5 人日，go-live 前完成）

> **本阶段不开发任何新功能。** 下周学员就要用，这一周只做「别出事」。

| 项 | 内容 | 人日 |
| --- | --- | --- |
| 0.1 | 补齐 P0 全链路浏览器验证：成绩（建考→录分→成绩单→通知）、销售（建线索→跟进→转化→渠道统计）、学生（新增→编辑→导入含错误行→导出）、签到联动（缺勤→通知生成 + 课时扣减） | 2 |
| 0.2 | ~~`git init` + 首次提交~~ → **已完成**（2026-09-12 建立，见 `PROGRESS.md`「Git 版本管理」）。剩余：确认 commitlint / husky 钩子生效 | 0.25 |
| 0.3 | `server/.env.example` 补建（后端环境变量模板，**属门禁 B1 的一部分**）；浏览器标签页标题改为系统名（当前仍是 `pure-admin-thin`） | 0.25 |
| 0.4 | 生产数据初始化：真实班级 / 学员 / 课程 / 课表 / 员工账号导入演练 | 1 |
| 0.5 | **备份恢复真机演练**（恢复一次并校验数据，不是只看备份存在） | 0.5 |
| 0.6 | 员工培训一页纸操作卡（签到 / 请假 / 报班缴费 / 成绩录入 四条主路径） | 0.5 |
| 0.7 | 上线首周现场值守与热修窗口预留 | 1 |

#### Phase 0-A · 上线门禁（🔴 必须完成才能发布）

> 来源：`docs/上线评估报告-2026-09-12.md`（全量代码审查）。
> **状态（2026-09-12 13:40）：Gate 1 全部 4 项 + Gate 2 全部 5 项已修复并通过验证（e2e 80/80、analytics 22/22、专项 35/35、ESLint 0 错、构建通过）。**
> 上线前仍需完成下方「部署侧动作」。

**✅ 已完成的代码侧门禁**

| 编号 | 门禁项 | 修复要点 | 证据 |
| --- | --- | --- | --- |
| **B1** 🔴 | JWT 密钥缺失即拒绝启动 | 新增 `server/src/config.js` 集中校验（位于建表之前）；`docker-compose.yml` 用 `${JWT_SECRET:?}` 强制注入；`server/.env.example` 补齐 | 无密钥启动退出码 1 且未触库；旧默认密钥伪造 token 被 401 |
| **B2** 🔴 | 线索转化明确为「仅建档」 | `docs/api.md` 改写语义 + 补录指引；前端转化弹窗加提示、修正标题；课时包/缴费由财务模块补录 | 文档与实现语义对齐；前端字段与后端一致 |
| **B3** 🔴 | 退班不再抹除已消耗收入 | `finance.js` 收入确认去掉 `status IN ('在读','结业')` 过滤 | 反事实对照 600 → 900，差额即被抹除的 300 元 |
| **B4** 🔴 | 财务边界校验 + 课时流水 | 金额 ≤1000 万、缴费/退费 >0、课时非负整数 ≤10 万；改 `remain_hours` 同事务写 `hour_consumptions`；退费不得超「已缴−已退/在途」 | 各类越界均返回 400；流水实测落库 |
| **H1** 🟠 | 前端刷新失败不再永久挂起 | 补 `.catch()` + 401 统一登出 + 错误文案全局提示 | ESLint 0 错；构建通过 |
| **H8** 🟠 | 删除请求体日志 | 只记 `method/path/status/耗时` | 日志实测无密码 |
| **H7** 🟠 | 安全加固 | `helmet` + `express-rate-limit` + CORS 白名单 + `trust proxy` | 安全头已下发；非白名单来源被拒；第 11 次错误口令 429 |
| **H6** 🟠 | **原判定为误报（已更正）** 审计覆盖本就完整；真实问题是 5 个模块重复写审计 → 全局中间件加 `SELF_AUDITED` 跳过 | 5 模块「写路由 : audit 调用」均 1:1 |
| **H2** 🟠 | 凭证可吊销 | 迁移 **v15** `users.token_version`；Token 带 `tv` 并比对；登出/改密/改角色即时吊销 | 登出后 access/refresh 均 401；迁移向后兼容 |

**🔻 上线前必须完成的部署侧动作（代码已就绪，需运维执行）**

| 项 | 动作 | 状态（2026-09-12） |
| --- | --- | --- |
| 1 | 生成并注入 `JWT_SECRET`：根目录 `.env` 已写入随机 64 位密钥（compose 自动注入，已被 gitignore） | ✅ 已完成 |
| 2 | **重建后端镜像**：`docker build -t attendance-system-server -f server/Dockerfile .` + `docker compose up -d --no-build` | ✅ 已完成（迁移 v15 已应用） |
| 3 | 重建前端镜像：`docker build -t attendance-system-web -f Dockerfile .` | ✅ 已完成 |
| 4 | 首次登录后**立即修改默认口令**（后端启动会打印默认口令告警；改密后旧凭证自动失效需重登） | ⏳ **待用户执行** |
| 5 | 运行 `bash server/scripts/docker-verify.sh` + `node server/scripts/e2e-lifecycle.mjs` 复核 | ✅ 已完成（9/9 + 80/80） |

**⏳ Gate 3 · 建议 go-live 后第 1 周内跟进**

H3（前端令牌改 HttpOnly Cookie）、H5（逻辑删除未过滤）、H9（清理文档/脚本中的明文默认口令）、H11（缺索引）、H12（前端 397 处 any）、H13（导入无上限）、H14（`xlsx` CVE）及全部 P2 项。

> ~~H10~~ **已于 2026-09-12 全面测试中修复**（销假回补查询去掉 `remain_hours>0`，0→1 回补复验通过，详见 `docs/全面测试报告-2026-09-12.md` F4）。
> 剩余 **H11 / H9** 建议紧随其后：H11 影响签到主路径，H9 清理成本极低。
> 修复 P1 级「缺索引」时**必须新增迁移脚本**（`server/src/migrations/0NN-*.js`，版本连续递增）并同步 `server/database.md`。

### Phase 1 · 数据地基（第 1–2 周，10 人日）

| 项 | 内容 | 人日 |
| --- | --- | --- |
| 1.1 | **F1 学员账户**：实缴 / 已扣课时 / 剩余学费，多订单分列汇总；学生档案新增「账户」页签；财务统计与首页新增「全校剩余学费总额（预收账款）」 | 4 |
| 1.2 | **F2 课堂评价**：课次批量评价（全班默认良好 + 只标异常）+ 单学生详评；维度见 §六 | 3.5 |
| 1.3 | **F3 知识点体系最小集**：学科 → 知识点树；考试 / 成绩录入时打知识点标签；历史成绩可补录 | 2.5 |

> **加速选项**：若希望 AI 更早见效，可在 Phase 1 末插入一个「简版学情报告」（仅基于成绩 + 考勤，1.5 人日），Phase 4 再用课评与知识点数据补全。

### Phase 2 · 排课治理（第 3–4 周，10 人日）

| 项 | 内容 | 人日 |
| --- | --- | --- |
| 2.1 | **F5 停课日历**：节假日 / 假期批量停课，自动跳过受影响课次并生成补课待办 | 2 |
| 2.2 | **F4 代课 / 换老师**：原教师 → 代课教师，课表与课消归属同步 | 2 |
| 2.3 | **F6 教室 / 场地**：教室资源 CRUD + 排课冲突检测（班级 / 教师 / 教室 三重） | 3 |
| 2.4 | **F7 整学期批量排课**：按周复制 / 按模板生成整学期课表 | 2 |
| 2.5 | 阶段回归与缓冲 | 1 |

### Phase 3 · 招生漏斗（第 5–6 周，10 人日）

| 项 | 内容 | 人日 |
| --- | --- | --- |
| 3.1 | **F8 试听 / 体验课**：试听预约排期、到课登记、试听后评价打分、试听→报名转化率统计 | 5 |
| 3.2 | **F9 线索运营**：公海池 + 分配规则 + 下次跟进时间提醒 + 我的待办 + 超时自动回收 | 5 |

### Phase 4 · 学情分析（第 7–9 周，15 人日）

| 项 | 内容 | 人日 |
| --- | --- | --- |
| 4.1 | **F15 LLM provider 抽象层**：`base_url` / `model` / `api_key` 环境变量可配；字段白名单脱敏；未配置自动降级规则引擎版；生成记录审计 | 2 |
| 4.2 | **F10 本地指标引擎**：出勤率、成绩趋势与波动、知识点掌握度、离群检测（同班 Z-score） | 4 |
| 4.3 | **F11 预警推送**：连续缺勤 + 成绩下滑 + 课时将尽 三类规则，站内推送 + 班主任待办 | 2 |
| 4.4 | **F12 学员学情报告**：结构化指标 + 图表 + DeepSeek 生成自然语言解读与建议，落库可回看、可打印 | 3 |
| 4.5 | **F13 班级学情概览**：谁在掉队 / 谁进步快 / 班级薄弱知识点，一页看完 | 2 |
| 4.6 | 全量回归（e2e + 浏览器 + Docker）与验收 | 2 |

### Phase 5 · 深化与工程治理（第 10–12 周，约 16 人日）

| 项 | 内容 | 人日 |
| --- | --- | --- |
| 5.1 | **F14 校长问数**（预置指标语义版，不做 text2sql） | 3 |
| 5.2 | `el-dialog` → 函数式 `ReDialog`、`el-table` → `@pureadmin/table` 逐页迁移（按 `WORKBUDDY.md` §0.5：一页一改、每页浏览器复验） | 5 |
| 5.3 | 移动端响应式：教师手机审批请假 / 查看今日课表 / 快速点名 | 3 |
| 5.4 | 报表增强：目标管理与达成率、同比环比、定时导出 | 3 |
| 5.5 | 缓冲与验收 | 2 |

### Phase 6 · 数据资产化与产出层（**2026-09-21 新增方向**，未排期）

> 依据：[[ADR-008-数据资产化与AI产出层架构]]（D15）。**Phase 6 不得早于 Phase 1–4**，因为产出层需要真实业务数据作燃料。
> **分批交付原则**：一个小场景验证成功 → 复用经验 → 再推广。**禁止一次性重构架构。**

| 项 | 内容 | 优先级 |
| --- | --- | --- |
| 6.1 | **文件资产表 + 磁盘目录**：新增 `assets`（或 `generated_assets`）表只存元数据（`student_id` / `type` / `file_path` / `size` / `created_at`）；建立 `server/data/assets/{students,classes,teachers}/` 目录结构，随数据卷备份 | 前置依赖 |
| 6.2 | **产出层第 1 步 · 数据表**：复用既有 `xlsx` 能力，把学情 / 成绩 / 考勤导出为规范表格（含样式与合并单元格） | 高 |
| 6.3 | **产出层第 2 步 · PDF**：后端渲染学员学情报告 / 成长路径 PDF，归档到 `assets/students/<学员>/` | 高 |
| 6.4 | **产出层第 3 步 · PPT**：`pptxgenjs` 或模板填充生成家长会 / 学期总结 PPT | 中 |
| 6.5 | **Dify 知识库启用**：`dify/` 从「暂不需要」转规划启用（:8081）；整理「参考资料」类内容入库（教学法 / 话术 / SOP / 学科知识）；取数走 `/api/agent/*` 脱敏 | 中 |
| 6.6 | **样板工作流**：先做 1 个场景（如备课）跑通三道防线（脱敏 / 权限 / 降级），验收后再决定其余场景是否全迁 Dify | 中 |
| 6.7 | 磁盘容量评估与扩容（文本 32GB 够用；**若存视频需 100GB+**） | 随 6.2 评估 |

> **⛔ 本期明确不做**：视频生成（暂缓）；自建向量库 / embedding 管线（交 Dify）；把 AI 产出留在 Dify（必须落回系统）；MCP（固定流程用 REST 更可靠）。
> **✅ 采集铁律不变**：沉淀一切数据 ≠ 加重老师负担。**永不强制、嵌入既有流程、新增表单 = 老师不用。**

### Later（二期候选，未排期）

学员标签与分群、课时有效期与冻结/赠送、作业布置与提交、自定义报表、回收站与软删除、自定义角色权限、学员附件（合同/证件扫描）、家庭成员关联、转介绍关系链、渠道 ROI、考勤二维码签到。

### 前端视觉待办（2026-09-18 视觉改版遗留，非功能需求）

| # | 事项 | 背景 | 优先级 |
| --- | --- | --- | --- |
| F1 | 其余页面的 `el-empty` 换成 `AppEmpty` | 插画已全局隐藏、字号字色已统一，**视觉已一致**；差异仅在组件统一与文案质量（`AppEmpty` 要求写清「为什么空 / 下一步做什么」） | P3 |
| F2 | 错误页 403/404/500 精细化 | 目前保持原结构，仅继承全局 token；Tailwind 的 `text-gray-500` 未换成 `--ink-500`（色差不可辨识） | P4 |
| F3 | `/ai-admin` 加入标签页 | pure-admin 的标签逻辑基于菜单匹配，而本页刻意不进菜单，导致离开后无法从标签栏返回。**属框架既有行为、非本次改版引入**；修法需动 `router/index.ts` 守卫，回归风险高于收益 | P3 |
| F4 | 深色模式浏览器验证 | token 与组件覆盖已同步深浅两套取值，但未做过验证（用户明确暂不考虑深色） | P4 |

> 视觉规范与遗留全量清单见 `docs/前端视觉规范-2026-09-18.md`。

### 内容清点待办（2026-09-21 发现，**未排期，待用户逐项拍板**）

> **来源**：排查 413 上传问题时顺带发现的「界面显示了不来自数据库的内容」。
> ⚠️ **本次仅抽查，非全量**。全系统逐界面清点**尚未执行**（见 C4）。
> ★ 立此清单的直接原因：C1 在 **2026-09-14 就已被发现**，却被记入 `PROGRESS.md`（**已完成**文档）→ 从此失踪 7 天。**待决策事项只进本文件，禁止写进 PROGRESS。**

| # | 事项 | 背景（含已核查的事实） | 优先级 |
| --- | --- | --- | --- |
| C1 | 顶栏通知铃铛接真实数据 | `src/layout/components/lay-notice/index.vue:8` 为 `ref(noticesData)`，**纯静态从不请求后端**。内容是模板自带演示数据（"小铭 评论了你"、"指派小铭于 2024-06-18 前完成更新并发布"、"开发多租户管理" —— 最后这条尤其离谱，多租户是明确不做项）。<br>★ 另含 **3 个第三方图片外链** `https://xiaoxian521.github.io/...`（**违反 ADR-002 不出网**，内网部署必裂图）。<br>★ **后端 `/api/notices` + 前端 `src/api/attendance.ts:267-287`（5 个函数，含 `latestNoticeList`）均已就绪** —— 只差替换数据源。<br>❓ **待产品决策**：铃铛有「通知/消息/待办」三个 tab，但本系统只有「通知公告」一个真实概念，另两个 tab 无承载物 | ✅ **已完成**（2026-09-21，见下方「通知铃铛 + 待办功能」L1） |
| C2 | 用户头像 | `users` 表**无 `avatar` 字段**；`server/src/routes/auth.js:342,416` **硬编码 `avatar: ""`**；前端 `useNav.ts:43` 空值回落内置静态图。属**全链路缺失**（表→接口→UI），不是"差一个上传按钮" | P2 |
| C3 | 个人中心 / 自助改密码 | 右上角用户下拉**只有「退出系统」**一项，无个人中心；`auth.js` **无改密码端点**。→ **老师无法自己改密码**，只能管理员重置。员工端系统的硬伤，且与「上线必改默认口令」的安全要求直接冲突 | P1 |
| C4 | 全系统逐界面内容清点 | 逐个界面确认「显示的值来自哪」，捞出同类问题（写死文案 / 假数据 / 永远为空的字段 / 越界外链）。**本次未做**，仅抽查出 C1–C3 | P2 |
| C5 | 工作台看板「待办：followUps」疑似真实模式仍显示演示数据 | `views/Dashboard.vue:24` 用 `engine.recentRecords(3)`，而 `ai/engine.ts` 直连 `@/mock/dataset`；**适配层 `workbench-data.ts` 没有 `followUps` / `recentRecords`**。→ 真实模式下该区块到底是演示数据泄漏还是引擎回退，**待核实**。属 `C4` 同类问题的具体一例 | P2 |

> 处置约定：**只列清单，等用户指定再修**。已在 `feature-dev-flow` skill 中固化为强制门禁。

---

## 五之二、通知铃铛 + 待办功能（2026-09-21 启动，**🔄 进行中**）

> **来源**：上面 `内容清点待办 C1`（铃铛演示数据）。
> **过程**：按 `feature-dev-flow` 走 —— ① 需求澄清（调 `grill-me`，共 11 轮追问）→ ② 扫描 + 横向同类扫描 + 开发计划 → 用户逐项拍板。
> **本节即 ① ② 的产出物**（《需求确认》+《开发计划》），按流程先归档再动手。

### 要解决的业务问题

| # | 问题 | 严重性 |
|---|---|---|
| 1 | 铃铛显示模板演示数据（"小铭 评论了你"、"指派小铭于 2024-06-18…"、"开发多租户管理"），**上线后员工会看到这些假消息** | 上线即事故 |
| 2 | 系统**已有公告**（5 接口 + 管理页）却**没有任何入口送到眼前** —— 公告等于"发了没人看" | 已有资产闲置 |
| 3 | 系统**没有待办概念**。业务风险散落各页（学员余额将尽 / 连续缺勤 / 线索未跟进 / 课评欠录），**没有机制告诉人"今天该做什么"** | 功能真空 |

### 铃铛最终形态（**2 个 tab**）

| tab | 内容 | 数据源 | 批次 |
|---|---|---|---|
| **通知** | 管理员发布的公告（全员可见） | `notices`（现成） | **L1** |
| **待办** | 我要做的事（系统生成 + 手工 + 指派） | `todos`（新建） | **L2 / L3** |

★ **「消息」tab 砍掉**。09-14 原以为"通知记录 `notifications`"可作消息，核查后发现它本质是 **「已发送的家校通知存档」**（发件记录，非收件箱；`target_user_id` 在迁移中定义但**全项目路由从未使用**，是死字段），放进铃铛会误导用户，且与「待办」定位重叠。它继续留在自己的页面 `family/notifications`。

### 分三批（用户拍板：分批推进，每批独立验收）

| 批次 | 内容 | 状态 |
|---|---|---|
| **L1** | 铃铛 3 tab → **1 tab「通知」** 接真实公告；**删除 `data.ts`**（3 个外链随之消失）；红点 = **最近 7 天新增公告数**（时间基准，不新建已读表） | ✅ **完成**（2026-09-21）<br>自测 `ui-bell-notice.py` **9/9** · 用户浏览器实测通过 |
| **L2** | 待办·手工：迁移 `019-todos.js` + `routes/todos.js` + 教务端页面 + **工作台新增菜单页** + `middleware/auth.js` 放行 `/api/todos`（`ai_agent`）+ 铃铛恢复「待办」tab | ✅ **完成**（2026-09-21）<br>API **15/15** · UI(admin) **14/14** · ★ 权限差异(教师 vs 管理员) **15/15** |
| **L3** | 待办·自动：幂等生成器 + 4 类事件（余额/课时预警、连续缺勤、线索待跟进、课评/知识点欠录）+ 触发（A 打开时 + C 定时） | ⬜ 待做 |

### 关键决策（本次拍板，勿轻易推翻）

- 待办给 **两端**用（校区负责人 + 一线老师）→ **一张 `todos` 表 + 按角色过滤**（非两套表）
- 待办来源：**自动 + 手工**；手工权限 = **自己建 + 管理员可指派**
- 自动生成时机：**A 按需幂等 + C 定时**。⚠️ 用户曾选 A+B+C，但 **B（实时）侵入性高而可观察差异近零**（生成器幂等 + A 已保证"一打开就是最新"），**L3 开工时再最终确认是否做 B**
- 工作台待办入口 = **新增菜单页**（与现有 10 个菜单并列；工作台无铃铛）
- L1 红点 = **时间基准**（不新建 `notice_reads` 表）

### 明确不做（边界）

- ❌ 任何自动发送（ADR-002 不出网）· ❌ 提醒推送（邮件/短信/浏览器）· ❌ 子任务 / 依赖 / 甘特 / 协作评论 · ❌ 待办跨端实时推送

### L1 改动清单

| 文件 | 动作 |
|---|---|
| `src/layout/components/lay-notice/index.vue` | 3 tab → 1 tab；`ref(noticesData)` → 调 `getNoticeList` |
| `src/layout/components/lay-notice/data.ts` | **删除**（含 3 个 GitHub 外链） |
| `src/layout/components/lay-notice/components/NoticeList.vue` | 适配真实数据形状 |

**L1 不需要改**（已逐项核查确认，非遗漏）：DB 迁移 · 后端接口 · 三份 nginx · 两种 docker-compose · 权限放行

---

## 六、关键设计决策（落地前需确认口径）

### 6.1 学员账户口径（建议值，上线前请校长确认一次）

以**实缴**为基准计算，避免欠费导致账面余额虚高：

```
实缴金额 paid          = SUM(payments.amount) - SUM(已审批通过的 refunds.amount)
已消耗金额 consumed_amt = paid × (consumed_hours / total_hours)
剩余学费 balance        = paid - consumed_amt   (= paid × remaining_hours / total_hours)
剩余课时 remaining_hours = total_hours - consumed_hours   （已有字段）
```

- **多订单**：每个订单独立计算，再在学员层面汇总；列表逐单展示，避免家长质疑时说不清
- **欠费部分**（应收 − 实缴）**不计入**剩余学费，单独在「欠费统计」体现
- **全校剩余学费总额 = 预收账款（负债）**，建议放到首页看板显眼位置——这是校长最容易误判的数字
- 现有 `/api/finance/stats/consumption` 的 `revenue_recognized = amount × consumed ÷ total_hours` 与上述口径一致，可复用

### 6.2 课堂评价维度（建议）

- **交互设计**：课次维度批量评价，全班默认「良好」，老师**只标异常学生**，大幅降低录入负担（否则必然烂尾）
- **结构化维度**（1–5 分，可选填）：专注度、参与度、作业完成、知识点掌握
- **自由文字**：一句话点评（可选，供 AI 生成报告时引用）
- **数据落点**：新建 `class_evaluations` 表（课次 + 学生 + 教师 + 维度分 + 文字 + 时间）

### 6.3 知识点体系最小集

- 表结构：`knowledge_points(id, subject, parent_id, name, sort)` 树形；`exam_scores` 关联知识点（成绩录入时按题目/题型打标签，或整卷打 1–3 个知识点）
- **只建树和标签**，不做题库、不做组卷、不做在线答题
- 历史成绩提供「补录标签」入口，否则 Phase 4 只能用新数据

### 6.4 AI 脱敏与降级

- **出网白名单**：只允许 `学号（内部 ID）+ 年级 + 结构化指标数值 + 知识点名称` 出网；**姓名、家长姓名、家长电话、金额一律不出网**
- **降级**：未配置 `LLM_API_KEY` 或调用失败 → 自动使用规则引擎模板版报告，页面明确标识「规则生成（未启用 AI）」
- **审计**：记录生成人、时间、模型名、输入指标快照，可复现

---

## 七、每阶段 Definition of Done（沿用项目强制约定）

- [ ] 数据库变更：新增迁移脚本（`server/src/migrations/0NN-*.js`，版本连续递增）并同步 `server/database.md`
- [ ] API 变更：同步 `docs/api.md`（权威）与 `docs/openapi.yaml`
- [ ] 前端：接口集中在 `src/api/*.ts`，views 内无 axios / `$route` / `localStorage`
- [ ] 浏览器验证：每个功能 Playwright 走查，截图入 `evidence/`
- [ ] 回归：`node server/scripts/e2e-lifecycle.mjs` 全绿 + `analytics-smoke.mjs` 全绿
- [ ] 构建：`pnpm lint:eslint` 0 error + `pnpm build` 退出码 0
- [ ] 部署：Docker 真机验证（`docker-verify.sh` + 生产包浏览器验证）
- [ ] 文档：`PROGRESS.md` 回填本阶段完成项与已知问题

---

## 八、风险登记

| # | 风险 | 影响 | 缓解措施 |
| --- | --- | --- | --- |
| R1 | **课评成为负担 → 老师不填 → AI 无燃料** | AI 学情分析失效 | 批量 + 只标异常；与点名提交流程绑定（提交点名后顺手评）；每周统计填写率 |
| R2 | AI 生成内容不可控 / 出现错误结论 | 误导教学决策 | 数字全部本地算；模板约束输出；教师可编辑后再发布；页面标注「AI 生成」 |
| R3 | 脱敏不到位导致学生信息外泄 | 合规风险（未成年人个人信息） | 字段白名单硬编码在 provider 层；出网前统一过滤；审计留痕 |
| R4 | 上线首周出故障 | 影响正常教学 | Phase 0 备份恢复真机演练；保留热修窗口；准备回滚方案 |
| R5 | 范围蔓延（知识库 / 题库 / 提成 / 家长端） | 工期失控 | §二 Non-goals 写死；任何新增需求先判断是否推翻 §〇 决策 |
| R6 | 单校区假设被推翻（开分店） | 架构返工 | 明确不为多校区预留；但保持 schema 可迁移（所有表已有单一 id 主键） |
| R7 | 剩余学费口径争议 | 钱算错，家长投诉 | §6.1 口径写死在文档；上线前让校长确认一次；UI 上展示计算式 |
| R8 | 历史成绩无知识点标签 | 学情分析初期数据稀疏 | 提供补录入口；Phase 4 允许「数据不足」的降级展示，不硬凑结论 |
