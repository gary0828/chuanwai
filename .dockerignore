node_modules
.DS_Store
dist
dist-ssr
*.local
.eslintcache
report.html

yarn.lock
npm-debug.log*
.pnpm-error.log*
.pnpm-debug.log
tests/**/coverage/

# Editor directories and files
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
tsconfig.tsbuildinfo

# 后端依赖与数据（数据通过 docker-compose 卷挂载）
server/node_modules
server/data
.trae

# AI 教学工作台：构建阶段重新产出，本地产物不入上下文
ai-workbench/dist
ai-workbench/node_modules

# Dify：源码与运行时数据卷体量大，仅在独立部署时使用
.dify-src
dify/volumes

# 本地验证与截图（含真实学员信息，绝不进构建上下文）
evidence
_verify_test
.workbuddy
.git

# 真密钥文件不进构建上下文。
# ★ 只排除 .env（含 JWT_SECRET 等真密钥）与 *.local；构建期仍需 .env.development /
#   .env.production 等 VITE_* 变量 —— 它们不含密钥、必须保留在上下文中，否则前端会白屏。
.env

# 开发期记忆系统（memory/）与 Agent 入口约定文件 —— 只服务开发环节，不属于运行时产物。
# 各 Dockerfile 都是白名单式 COPY（前端只取 dist / ai-workbench/dist，后端只取 server/src），
# 本就带不进镜像；这里再加一道保险，同时减小构建上下文体积。
memory
AGENTS.md
CLAUDE.md
.cursorrules


