# WORKBUDDY · 项目硬约束

> 本文件是 AI 在本仓库工作的**最高优先级规则**，且必须在根目录才能被工具自动加载。
> 它**只放硬约束**，细节一律放 `docs/` 并留指针，避免与 `docs/03-开发指南/` 重复。
>
> 会话启动流程在 `memory/INDEX.md`（唯一入口），**本文件不再整读**，按需取章节即可。
>
> 事实基线：2026-09-21 ｜ 数据库 **v19** ｜ 回归：全页面巡检 28/28 · e2e 80/80 · analytics 22/22 · P0 浏览器 31/31 · Docker 9/9 · 业务链 49/49

---

## 1. 项目定位（不可违反）

- 培训机构**纯员工端**系统：`admin` / `teacher` 两种员工角色；**学员与家长无账号、不登录、不出网触达**。
- 单校区自用，规模：学员 200–500 / 员工 5–15。
- 系统是**唯一数据源、也是唯一仓库**；Dify 只是加工厂（只读）。见 [ADR-008](docs/07-架构与决策/ADR/ADR-008-数据资产化与AI产出层架构.md)。

## 2. 推进顺序（2026-09-20 用户拍板）

**教务闭环优先于一切新功能。** AI 工作台是下一步，开工前须先收集校区老师真实使用意见；教务系统里「逻辑不顺畅 / 数据不通 / 关联做不好」的问题优先修。

## 3. P0 模块（未达标前禁止投入其它功能）

| 优先级 | 模块 | 验收标准 |
| --- | --- | --- |
| P0 | 考勤登记 | 选日期 + 班级 + 课程 → 加载名单 → 逐人标记 → 批量提交，无报错 |
| P0 | 学员管理 | 增删改查 + Excel 导入导出 + 按班级筛选，教师仅见本班 |
| P0 | 成绩管理 | 考试创建 → 成绩录入 → 成绩单（排名 + 等级）→ 家长通知 |
| P0 | 招生线索 | 登记 → 跟进 → 状态流转 → 一键转化（自动建档 + 报班） |

每天收工前跑一次全量 e2e 回归 + 浏览器冒烟。

## 4. 跨模块业务联动（改相关模块后必查）

| 触发操作 | 应联动 | 预期效果 |
| --- | --- | --- |
| 考勤标记为正常 / 迟到 / 早退 | 课时包 | 扣减 1 课时 + 写消耗流水 |
| 考勤标记为缺勤 | 家校通知 | 生成缺勤通知（家长姓名快照） |
| 缺勤改为正常 | 通知 + 课时包 | 撤销当日通知 + 扣减 1 课时 |
| 考勤标记为请假 | 请假 | 自动生成待审批单（幂等） |
| 补课标记完成 | 课时包 | 扣减 1 课时 |
| 线索转化 | 学员 + 订单 | 事务创建档案与报班订单 |
| 成绩录入（新插入） | 家校通知 | 推送成绩发布通知（修改不重复） |
| 订单改为结业 | 经营报表 | 在读率 / 续班率更新 |

## 5. 数据库迁移（不可违反）

1. schema 变更**只能**新增迁移脚本 `server/src/migrations/0NN-*.js`，**禁止修改历史迁移**；版本号必须连续（`m.version === current + 1`，跳级会终止启动）。
2. 每次迁移后同步 `server/database.md`。
3. 命名：表名小写下划线复数；布尔 `is_xxx` / `has_xxx`；时间 `created_at` / `updated_at`；外键 `xxx_id`（必建索引）。
4. 跨表写操作必须走事务；有业务关联的数据禁止硬删除，改为状态字段 + 400 提示。
5. 脚本必须导出 `version` / `name` / `up(db)` / `down(db)`。

> 细节与踩坑见 [后端与数据](docs/03-开发指南/后端与数据.md)。

## 6. API 与文档同步（不可违反）

- 唯一权威：[`docs/04-API/API.md`](docs/04-API/API.md)；机器可读：[`docs/04-API/openapi.yaml`](docs/04-API/openapi.yaml)。
- **每次新增 / 修改接口必须同步这两份**。
- 部署 / 环境变量变更 → `docs/05-配置/`、`docs/06-部署/`、`server/.env.example`。
- 发版 → `CHANGELOG.md`；会被质疑的决定 → 新增 ADR（只追加不修改）。

## 7. 测试要求（不可违反）

**不能只跑 API 测试就认为功能可用**，涉及界面的改动必须在浏览器里实际走通。

- 后端改动 → e2e + analytics 冒烟 + 业务链探针。
- 前端改动 → `pnpm lint` + 生产构建 + 全页面冷启动巡检（**改路由 / 菜单后必跑**）。
- 交付前 → Docker 完整跑一遍 `bash server/scripts/docker-verify.sh`。
- 浏览器验证用**生产构建产物**，不要用 vite dev server。

> 脚本清单与断言铁律见 [测试与验证](docs/03-开发指南/测试与验证.md)。

## 8. 前端规范

- 严格遵守 pure-admin 官方规范 → [pure-admin 规范](docs/03-开发指南/pure-admin规范.md)。
- 视觉一律走 `src/style/tokens.scss`，页面**禁止硬编码色值** → [前端视觉规范](docs/03-开发指南/前端视觉规范.md)。
- 教务端与 AI 工作台的差异 → [前端两端差异](docs/03-开发指南/前端两端差异.md)。

## 9. 数据与密钥红线

- 系统是唯一数据源与唯一仓库；**文件不入库**（索引进库、文件落盘 `server/data/assets/`）；AI 产出必须落回自有库，不留在 Dify。
- **任何密钥不得出现在前端任何位置**（localStorage / 打包产物 / URL 都不行）；模型调用一律走服务端 `POST /api/ai/generate`。
- `server/data/` 整个目录（含 `assets/`）一起备份，只备份 db 会丢文件。
- 仓库**公开**，推送前必须扫敏感数据（真实学员数据 / 密钥）。

## 10. AI 读取预算（Token 纪律）

1. **禁止整目录读取**：`node_modules/`、`server/data/`、`dist/`、`evidence/`、`_verify_test/`、`*.db*`、`pnpm-lock.yaml`（已由 `.aiignore` 覆盖）。
2. **用搜索代替遍历**：找功能先 Grep 关键词，不要先枚举文件。
3. **启动只读**：`memory/INDEX.md` → `memory/CURSOR.md` → 近两天日志 → 按需取 `docs/`。前两步 ≤ 6 KB，常规任务累计 ≤ 15 KB。
4. **禁止**启动阶段整读 `README.md`、`docs/08-参考/PROGRESS.md`、本文档全文、扫描源码目录。
5. 输出时不复述源码，报错只贴关键行 + 根因。
6. 批量删文件**每批核对数量**；误删用 `git restore --source=HEAD --staged --worktree -- .` 整体还原。删除依赖后必须同步 `pnpm-lock.yaml`。

> 环境坑与工具细节见 [环境坑与工具](docs/03-开发指南/环境坑与工具.md)。

## 11. 会话协议

1. **初始化**：按 `memory/INDEX.md` 的指针取文件（不要全项目扫描）；起后端；跑 e2e 确认基线全绿。
2. **任务优先级**：P0 缺陷 > 浏览器发现的 UI / 交互问题 > e2e 失败 > 跨模块联动不通过 > API 文档缺失 > 数据库不规范 > pure-admin 偏离 > 性能 / 安全 / 体验。
3. **执行**：一次只做一件事；前端改动前先确认符合第 8 节。
4. **循环**：完成一个任务后自动进入下一个，不等待确认；遇阻塞记入日志并跳过。
5. **收尾**：按 `memory/PROTOCOL.md` §2 五条清单归档（日志 + 覆写 CURSOR + 知识 / 任务 + 体检）。**没归档 = 这次会话在下一轮里不存在。**

## 12. 约束清单

- 不破坏数据库迁移历史。
- 不改变「纯员工端」定位。
- 不删除或覆盖未提交的他人工作。
- 所有 API 遵循现有响应格式与错误码。
- Docker 部署配置保持可用，交付前必须完整验证。
- **P0 模块优先于一切；每个功能都必须经过浏览器验证。**
