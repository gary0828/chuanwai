# 2026-09-26 日志

## 15:56 | Feature | 菜单重构（方案 A）+ 出勤率口径统一 + 备份实现统一

- 来源: 用户「好的，开始修改。」（承接 09-24 的扫描报告，采纳全部推荐项）
- 前置: 读 `CURSOR.md` + `feature-dev-flow/SKILL.md`；确认远端=本地 `7c8e0f8`，工作区含未提交的 v22 改名工作

### ① 菜单重构（`server/src/routes/auth.js` 的 `ROUTES`）

- admin 9 组 / teacher 6 组；**考勤管理 8→4 项**、**数据管理拆解消失**、新增「排课与课表」、
  「报班管理」并入「招生与报名」、「员工账号」移入「系统管理」、三处改名
- ★ **所有子项 path 未改**（老师收藏不失效）；已查证父子 path 无需前缀匹配
  （`getParentPaths` 存父级 path / `addAsyncRoutes` 不拼接 / 子路由按绝对路径独立注册）
- 新建 `docs/08-参考/…` 报告状态转「已实施」

### ② 出勤率口径统一（真重复 #1）

- 新建 `server/src/utils/attendance-rate.js`，收口 **9 处**内联公式
  （**横向扫描把原估 7 处改成 9 处**：漏了 `agent.js` 工作台出口、`attendance.js` 缺勤预警）
- 删 `analytics.js` 的 `attDenom`；**同步口径字典**（`METRIC_DEFINITIONS` + `API.md` 两处旧公式）
- 20301 组样本对拍证明与改造前数值一致；工作台 `engine.ts` 本就同口径（2:1 多数派，验证选型正确）

### ③ 备份实现统一（真重复 #2）

- `server/src/utils/backup.js` 的 `copyFileSync`+checkpoint → **`VACUUM INTO`**，与运维脚本同一实现
- 加固：同名先清、失败清半成品；实测产物 `integrity_check=ok`、外键 0 违规、与主库一致

### ④ 课程页「授课教师」→「默认授课教师」（展示标注）

### ⑤ ★ 修正一条**自己上一轮的误报**

- 09-24 报告称「课消统计菜单放行 teacher 但接口 403」→ 核实 **teacher 菜单根本不含财务管理**，
  菜单按 `ROUTES[role]` 下发 → **误报**，已在报告与 PROGRESS 中更正，本次取消该项
- 教训: **admin 菜单里的 `roles` 标记与 teacher 可见性无关**（两套菜单是独立内容）

### ⑥ 顺带修复：回归脚本「自污染 → 自己崩」

- `verify-sessions.mjs` 挪课测试硬编码 4 个候选时段且**不自清理** → 跑满 4 次后 `freeSlot=null`
  → TypeError 崩溃，**后半个脚本全没跑**（实测踩中）
- 处置: 动态搜索（未来 60 天）+ 排除脏课次（`related_session_id` 非空）+ 找不到就 SKIP
- 沉淀 K-047（"跑越多越红"是脚本自污染的指纹）

### 验证（统一入口 18080，镜像已重建并取证）

- 新增 `_verify_test/verify-menu-refactor.mjs` → **51/51**
- 新增 `_verify_test/ui-menu-refactor.py` → **32/32**（双角色 + 截图）
- e2e **80/80** · 课次 **90/91**（唯一 FAIL = 既有 B7）· openapi 147 ref exit 0
- 断言「新菜单覆盖重构前全部 32(admin)/14(teacher) 项」→ 用代码证明**没丢功能**

### 待决策（已写入 ROADMAP §11）

- S1 4 条历史测试残留的「挪课」课次（是否清理，需用户点头）
- S2 「挪课→停课→恢复」不清理 `related_session_id` → 该课次永久无法再挪（语义待定）
- S3 `analytics.js` 死变量 `range` · S4 server 全目录既有 prettier 报错 · S5/S6 可选合并与抽函数

### 关联

- K-044（菜单双套结构）· K-045（出勤率唯一口径）· K-046（备份 VACUUM INTO）· K-047（脚本自污染）· K-048（菜单渲染行为）
- 报告: `docs/08-参考/功能重复扫描与菜单重构方案-2026-09-24.md`
- 生产（服务器）**未动**；本地 18080 已更新等用户实测（门禁 3）

## 17:40 | Fix+Test | ★ 修复「批量导入学生无反应」（P0）+ 业务流测试

- 来源: 用户「推送到gitee，然后帮我做一个测试计划…用户反馈批量上传学生信息时没有任何反应…不要只是功能可以就行」
- 推送: 菜单重构那批已推 gitee（`d779d2b`，36 文件）

### ★ 用户报的 P0：导入完全静默 —— 已复现 + 定位 + 修复 + 验证

- **复现证据（修复前）**: 含错误班级的文件、**连完全正确的文件**、重复 3 轮 → 全部 **0 请求 / 0 消息 / 0 弹窗**
- **根因**: `before-upload` 的参数是 **UploadRawFile**（`extends File`，**无 `raw` 字段**；
  `raw` 只在 change 事件的 `UploadFile` 上），代码却读 `file.raw` → undefined →
  `readAsArrayBuffer(undefined)` **同步抛错**；而 try/catch 只包了 `reader.onload` 的**内部** →
  无人接管 → 无请求无提示。**参数标成了 `any` 才让类型检查没拦住**。
- **修复**: 传 `file` 本身 + 补三条反馈路径（reading 失败 / 业务 success:false / catch）+ loading + 防重复提交
- **连带修复 4 个同源缺陷**: ①模板「来源渠道/报名日期」未映射 → 静默丢数据（P1）
  ②无 loading（P2）③`.then` 无 catch、success:false 无 else（P2）④模板示例班级名不存在（P2）
- **验证**: 修复后 **PASS 29 / FAIL 0**；含错误班级→明确提示指明行号；重复导入→逐行列出「学号已存在」

### 业务流测试（按用户要求设计 6 条流程）

- 产出 `docs/08-参考/业务流测试计划-2026-09-26.md` + `业务流测试结果-2026-09-26.md`
- 脚本 `_verify_test/business-flow.py`（UI + 跨模块对账 + **自动清理测试数据**）、
  `repro-student-import.py`（复现诊断，可回归）
- 结果: A 线索→学员+订单联动 ✅（40→41 / 17→18）· B 教师端连续切 10 页零报错 ✅ ·
  C 财务统计实收 = 经营报表营收（22400=22400）✅ · F 报告可生成 ✅
- **测试数据已清理**（订单 2/学员 4/线索 1），学员总数回到基线 **29**

### 顺带发现（待用户决定）

- ★ 本地库有 **21 条历史测试残留学员**（`QA*`/`PBMU*` 前缀，非本次产生）→ **会计入统计分母与首页数字**
- 学习报告「考勤 0 次」与「暂无数据」未区分（P2）

### 关联

- K-049（before-upload 参数坑）· K-050（API 测试覆盖不到 UI 环节 · 静默即缺陷）
- 记忆教训: **"功能测试通过"≠ 用户能用**；本次缺陷 100% 在 UI 步骤上，接口测试天然测不到

## 18:50 | Fix | 修复「通知记录」表格固定列叠字（用户反馈「前面的内容无法覆盖」）

- 来源: 用户截图（通知记录页右侧标红，三列文字重叠）
- **量化复现**（1440 视口）: 容器 1090px vs 列宽合计 **1410px**（溢出 320px）；
  固定列（操作）x=1260~1370 **压在**「通知家长」(1210~1320) 与「状态」(1320~1410) 之上
- **根因（两条全局样式叠加）**:
  1. `element-plus-override.scss` 把 `--el-table-bg-color` / `--el-table-tr-bg-color` 设为 **transparent**
  2. fixed 列是 **sticky 悬浮贴纸**，靠自身背景遮住下方单元格 → 透明就**盖不住** → 叠字 ✅ 正是用户说的"无法覆盖"
- **修复**: 全局给 fixed 单元格不透明底色（**必须 `!important`**，EL 的 `td.el-table__cell` 特异性更高，
  实测不加无效）+ hover/striped 跟随；`通知记录` 列宽 1410 → **1090**（0 溢出）
- **验证**: 列宽合计 = 容器 = 1090，固定列背景 `rgb(255,255,255)`，重叠 **0px**，
  全部单元格单行（关联日期给 108px 才不折行）
- ★ **横向扫描 12 个表格页**: **固定列透明 = 0 个 → 叠字已全局根治**；
  仍有 5 页列宽溢出（报班管理 +310 / 学生管理 +150 / 缴费记录 +85 / 考勤记录 +40 / 补课管理 +20），
  但**已不混乱**（有背景 + 可横向滚动）→ 记入 ROADMAP **S7** 待用户定（报班管理 14 列属信息架构问题）
- 沉淀 **K-051**（fixed 列背景坑 + 列宽经验）· 新脚本 `_verify_test/scan-table-layout.py`

## 19:30 | Audit | 「功能做精」审计（用户：「不是扩展功能，是把每个功能做精，还能怎么做」）

- 方法: 2 个并行审计通道（考勤链 / 学员财务链），逐功能对照**五问**：
  ①能否后悔（撤销/删除）②数字能否追到明细 ③删除是否告知代价 ④高频能否批量 ⑤跨模块影响有无守卫
- 产出: `docs/08-参考/功能做精审计-2026-09-26.md`（60+ 处可做精，按三批排序）
- ★★ **最危险的不是缺功能，是「静默改错数据」**：
  1. **改一条考勤会新增重复行** —— 记录页「修改」不带 `session_id`（`records/index.vue:107-117`），
     而考勤是**双分区唯一索引**（`migrations/021:146-147`）→ 走 legacy upsert **新插入一行** →
     统计 `COUNT(a.id)` 算两遍、课时按另一分区状态多扣/多回补（**越订正越脏**）
  2. **删课程静默清空订单的 course_id**（`courses.js:85-110` 漏检 `orders`/`teaching_assignments`）→
     该订单学员**以后点名永不扣课时**。同类：删学期级联删任课关系（`terms.js:111-122`）、
     删订单清空课消流水（`finance.js:280-289`）
  3. **财务「已缴/实收」未减退费**（`finance.js:89 / 541 / 603-604`），
     与**自家 ADR-003 定义冲突**（`ADR-003:30`）+ 「剩余学费/预收账款」至今未落地
- 其余重点: 请假审批/退费审批/挪课/停课/退班**全无撤销**；学生-报班-缴费**全无批量**；
  课消**只有聚合无明细**（表里有 operator_id/date/session_id 却不展示）；
  报班课程可空 → 订单永不扣课时却无提示；筛选条件刷新即丢；导出静默截断
- 沉淀 **K-052**（删除保护覆盖所有外键引用；`classes.js:164-182` 是范本）· ROADMAP **S8** 待用户选批次
- 未改任何代码（纯审计）
