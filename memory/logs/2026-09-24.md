# 2026-09-24 日志

## 08:35 | Question | 用户质疑「新功能太复杂看不懂」+「是否和数据管理重复」

- 来源: 会话（用户原话「你解释一下你新做的那几个功能都是干什么的？我看起来怎么这么复杂？看不懂。并且我觉得你新作的功能是不是和数据管理里面的有些内容重复了？」）
- 影响: 无代码改动（本轮只做解释 + 比对）；产出一个「模板 vs 实例」图解 widget
- 关联: G1+G2（v21 课次实体 / 任课关系）

### 核查结论（已读代码验证，不是凭印象）

**「数据管理」到底是什么** —— 它是 `server/src/routes/auth.js:180/344` 里的一个**菜单分组** `/data`，
不是功能模块。内含（admin）：班级管理 / 学生管理 / 课程管理 / 课程表 / 学期管理 / 调课审批 / 补课管理；
（teacher）：班级管理 / 学生管理 / 课程表 / 补课管理。

**逐项比对**

| 新功能 | 数据管理里的邻居 | 结论 |
|---|---|---|
| 周课表（sessions） | 课程表（schedules） | ❌ 不重复：**模板 vs 实例**。schedules 一条记录 = 每周重复的一节课；class_sessions 一条 = 某天某节具体的课 |
| 任课关系 | 课程管理里的「授课教师」 | ❌ 不重复：`courses.teacher` 是**手填文本**（`src/views/attendance/courses/index.vue:31`），不与 users 关联；teaching_assignments 是结构化关联表。证据：schedules 页冲突检测用 `r.teacher || ""` 做**文本比较**（`schedules/index.vue:174`） |
| 节次时间 | 学期管理 | ❌ 不重复：一个是学期起止，一个是第 N 节的钟点 |
| 回填报告 | 数据备份（系统管理） | ❌ 不重复：备份 = DB 文件；回填报告 = 历史数据挂课次的成功/失败清单 |

### ⚠️ 唯一一处真重叠（未解决，待用户拍板）

**「调课」有两个入口、两套语义，都叫调课：**

1. `数据管理 → 调课审批`（`server/src/routes/adjustments.js`）：teacher 申请 → admin 审批 →
   通过后 **UPDATE schedules**（`adjustments.js:146`）→ **改的是模板，以后每周都变**
2. `考勤管理 → 周课表 → 课次详情 → 调课`（`POST /sessions/:id/reschedule`，admin 直接操作）：
   原课次标「已调课」+ 新建新课次 + 双向关联 → **改的是某一节，不影响以后**

语义确实不同，但**术语撞车**，用户会困惑。已向用户提出三个选项（改名 / 审批通过后联动停课次 / 把单次调课并入审批流）。

### 另一个易混点（未解决）

周课表挂在「考勤管理」下，课程表挂在「数据管理」下 —— 两个都长成星期网格，
但数据不同。建议要么改名（课程表→「排课模板」），要么把周课表挪到「数据管理」里与课程表并列。

## 09:05 | Decision | 用户拍板两套改名，已实施（迁移 v22）

- 来源: 会话（用户在两个选项里都选了推荐项，并接受一次数据迁移）
- 影响: 17 个文件 + 新迁移 `server/src/migrations/022-session-reschedule-rename.js`
- 关联: G1 课次实体 · K-043

| 原名 | 新名 | 范围 |
|---|---|---|
| 课程表 | **排课模板** | 菜单（admin+teacher）、页头、弹窗标题、后端 403 文案 |
| 课次「调课」 | **挪课** | 课次详情全部文案 + **DB 枚举值** |

**★ 关键实现约束（下次照做）**：SQLite 的 `CHECK` 对 `UPDATE` 同样生效 ——
「先 UPDATE 旧表把 `已调课` 改成 `已挪课`，再重建表」**跑不通**，第一次执行就
`CHECK constraint failed` 并整体回滚。正确做法：在 `INSERT INTO ..._new SELECT` 时
用 `CASE status WHEN '已调课' THEN '已挪课' ELSE status END` 完成映射，建完再补一条
「旧值残留 = 0」的断言兜底。

**未改（有意保留）**：`数据管理 → 调课审批`（改长期模板，语义不同）· `schedule_adjustments`
相关文案 · `ADR-009`（只追加不改）· `ROADMAP.md` · 迁移 004 / 021（历史记录）。

## 09:30 | Done | 改名已交付本地环境，等用户实测

- 补充产出 `server/docs/022-课次挪课改名-model-review.md`（**pre-commit 硬拦**：新迁移必须配
  `server/docs/<版本号>-*-model-review.md` 且含「结论：通过」，见 `.husky/pre-commit`）—— 结论：通过
- 本地 docker 两步法重建完成（K-040 取证：容器镜像 ID = 刚构建的 `9ba2fcb1…` / `ab59f950…`），
  容器内库已 v22，状态分布含「已挪课」
- 验证：`verify-sessions.mjs` **96/97**（B7 为既有 P1 缺口，已在 v21 备份库独立复核确认同样失败）·
  UI 回归 **42/42** · 定向改名检查 **6/6** · `check-openapi` exit 0 · 前端构建成功
- **未提交 git**（等用户实测通过）；遗留 P1：`server/database.md` 第 39/41 行版本清单还停在 v21

## 08:40 | Note | 用户理解门槛：新功能的解释要「先讲缺什么」再讲「做了什么」

- 来源: 会话（用户说「看不懂」）
- 教训: G1/G2 这类**地基型**改动，PRD 里写的是「class_sessions 表 + 状态机 + 双条件索引」，
  但对用户来说这些是内部实现。正确讲法：**先说原来的系统缺哪一维 → 由此产生什么做不了的事
  （含真实故障）→ 再说补了什么**。
- ★ 真实故障（最有说服力，以后复用）：
  1. 考勤唯一键 `(student_id, course_id, date)` → **同一天同一课程上两节，第二节点不进名**
  2. 没有「谁教哪个班哪门课」的表 → **任课老师登录后自己教的班什么都看不到**（只有班主任能看）

## 11:58 | Doc | 服务器升级 · 小白照抄手册（用户：「现在我要给服务器做一个更新，怎么做，小白」）

- 来源: 用户主动要求给**生产服务器**做更新，自述小白
- 前置确认（三问）: **SSH 命令行** · 项目在 **/opt/jx-system 英文路径** · **有真实学员数据、老师已在用**
- 产出: `docs/06-部署/服务器升级-小白操作手册.md`（照抄式：〇 快照保险 → 一 现状 → 二 备份 → 三 拉码 →
  四 构建 → 五 切换 → 六 验证 6 项 → 七 强刷 → 八 回滚 → 九 报错表 → 十 本次变化 + 一页纸附）
- **两个关键改进（已回写 `校区部署与升级.md` §5）**：
  1. **低停机**：`docker compose build`（**不停机**，旧容器照常服务）→ 再 `up -d --no-build --force-recreate`
     （停机仅 10–30 秒）。原文档直接 `--build --force-recreate` 会把 3–8 分钟构建时间算进停机窗口
  2. ★ **`git remote -v` 陷阱**：项目**只推 gitee**，github 那份是**旧版** → 服务器若 origin=github，
     `git pull` 会静默拉到旧代码（"升级成功"但版本没变）。必须 `git pull https://gitee.com/gary0828/chuanwai.git main`
- 给小白的回滚优先级：**PVE 虚拟机快照 > 命令行 restore**（迁移单向，程序+数据必须一起回）
- 本次目标版本: gitee main `2c6088b`，DB **v21**
- 生产**仍未实际操作**（用户自己执行，无 SSH）｜关联: K-020 K-040 K-027
