# 2026-09-23 日志（L1 · append-only）

> 格式见 `../PROTOCOL.md` §3。**只追加、不改昨天**；要修正就追加一条 `Correction:`。
> 上限 6 KB/天。高信号条目在会话末 Promote 进 `knowledge/_index.md` 或 `tasks/`。

---

## 09:20 | Fact | 服务器 18080 整页白屏的根因 = `.env.production` 被 gitignore 吞掉

- 来源: 用户报障（昨天服务器部署，白屏）+ 现场控制台报错
- 症状: `http://192.168.1.222:18080` 白屏；控制台 `Uncaught TypeError: Cannot read properties of undefined (reading 'split')  at index-<hash>.js`
- **根因（已用代码 + git 双向证实）**:
  1. `src/router/index.ts:77` 模块加载即执行 `createRouter({ history: getHistoryMode(import.meta.env.VITE_ROUTER_HISTORY) })`
  2. `src/router/utils.ts:338-340`：`getHistoryMode(routerHistory)` **无默认参数**，直接 `routerHistory.split(",")` → 传 undefined 必崩
  3. 为何 undefined：`.env.production` 定义 `VITE_ROUTER_HISTORY="hash"`，但 `.gitignore:20` 的 `.env.*` 把它一起忽略了（`!.env.example` 只放行样例）；`git check-ignore` 证实 `.env.production/.env.development/.env.staging` 全部 ignored 且 not tracked
  4. 触发路径：部署文档 `docs/06-部署/校区部署与升级.md:159/541` 用 **`git clone`** 取码 → 新克隆没有 `.env.production` → `vite build` 把 `VITE_ROUTER_HISTORY` 内联成 `undefined` → 崩
- **关键澄清（用户误判）**: 白屏与 `LLM_API_KEY` **无关**——它是后端变量、无 `VITE_` 前缀，Vite 根本不读它；「AI 配置中心」页面（`src/views/ai-admin/index.vue`，管理 llmApiKey/llmBaseUrl/llmModel/…）也已经存在且走 DB。这次炸的是 pure-admin 的**构建期路由变量**，属另一条线
- 同类扫描: 工作台 `ai-workbench/src/router.ts:117` 用**硬编码** `createWebHashHistory()`，无 env 依赖 → 无同类 bug；崩溃仅限教务端
- 影响面: 任何「git clone 到干净机器 + 本地 vite build」的部署都会白屏；本地开发（有 `.env.production`）与「拷整个目录」部署不受影响
- 待用户拍板: ① 构建期 env 是否入库 ② docker 统一到哪一形态 ③ LLM Key 是否彻底移出 .env

## 10:05 | Decision | 白屏修复 + Docker 统一 + LLM 归页面（用户三问拍板后实施）

- 来源: 会话（用户「开始」批准开发计划）
- 用户拍板: ① Docker 统一到「统一入口单端口」 ② 构建期 env 入库 + 代码兜底 ③ LLM 密钥移出 `.env`，只用「AI 配置中心」页面
- 实施清单:
  - **A 根因**: `src/router/utils.ts` `getHistoryMode` 加默认参数 `"hash"` + 非法值兜底返回；`.gitignore` 收窄为只忽略 `.env`/`.env.local`/`.env.*.local`，放行 `.env.development`/`.env.production`/`.env.staging`；三文件纳入版本库
  - **B 统一**: `docker-compose.yml` 改写为统一入口（服务固定 `image` 名 + `container_name`），**删除 `docker-compose.unified.yml`**；`docker-compose.verify.yml`/`deploy/linux-server-setup.sh`/`deploy.sh` 同步；`deploy.sh` 顺带修了「`.env.example` 无 `JWT_SECRET` 行 → 原 sed 空转、密钥从未写入」的旧隐患
  - **C LLM**: compose / 部署脚本删 `LLM_*` 注入；`ai-workbench/src/views/Settings.vue`、`server/.env.example`、`server/src/config.js` 注释改为指向「AI 配置中心」；本机 `.env` 删 `LLM_API_KEY`
  - **D 加固**: `.dockerignore` 新增排除 `.env`（真密钥不进构建上下文）
- 验证（4 项全过）:
  1. `getHistoryMode` 兜底单测：`undefined/null/""/垃圾` → 一律 hash、**零抛错**（原崩溃点）
  2. `pnpm build` 成功（EXIT=0）
  3. `docker compose config` 解析正常：`name=attendance-system`、`unified` 端口取 `WEB_PORT=18080`、**已无 `LLM_*`**
  4. Playwright 无头加载真实构建产物：**0 pageerror / 0 console.error**、登录页正常渲染、`#app` 有内容 → 无 `split` 崩溃
- 待办: 用户实测（门禁 3）→ 推送 gitee → 生产重建（K-020 单独问）

## 09:50 | Fact | 重部署会不会丢数据？—— 结论 + 文档补强

- 来源: 会话（用户：「服务器现在已经好了，但如果现在我要重新部署的话，服务器上的数据怎么办？」）
- 结论: **不会丢**。数据在宿主机 `<项目目录>/server/data/`（compose `- ./server/data:/app/data` bind mount，`server/Dockerfile` 另有 `VOLUME` 兜底），
  重建镜像 / 删容器 / `docker compose down`（不加 `-v`）都不影响；上传的 Logo 等在 `server/data/assets`，一并保留
- ★ **唯一真风险**: 另开**新目录** `git clone` 起服务 → 新目录没有 `server/data/` → 后端首启**自动建表 + 写种子演示数据**（老师会看到假班级），真数据仍留旧目录
- 附带: `.env`（含 `JWT_SECRET`）也在项目目录里且被 git 忽略 —— 换目录必须带上，否则密钥变、全员强制重登
- 本次无 DB 结构变化（仍 v19），不需要额外迁移
- 文档补强: `docs/06-部署/校区部署与升级.md` §5 升级五步法 —— 补「③ 先 `docker rm -f` 清旧同名容器」（本次 Docker 统一引入的**一次性**动作）+ 「必须同目录 pull，不要新目录 clone」说明
- 关联: K-026

## 10:35 | Decision | 落地「发现的两处遗留项」+ 推送 gitee

- 来源: 会话（用户「推到 gitee，但先把你之前发现的两处改了」）
- **① 验证脚本端口**（旧三端口 8080/8082 → 统一入口 18080）
  - 仓库内（repo-visible）：`server/scripts/docker-verify.sh` 改为按 `.env` 的 `WEB_PORT`（缺省 18080）——后端 3000 不再对外，FE/BE 同端口；`ui-icon-check.py`、`ui-growth-verify.py`、`ui-kp-units-shots.py`、`restore-db.sh`
  - 文档命令同步：`CONTRIBUTING.md`、`docs/00-导航.md`、`docs/03-开发指南/环境坑与工具.md`、`项目地图.md`
  - 本地 harness（`_verify_test/`，**gitignored**）：机械改 21 个文件；**跳过 4 个含 `:8082` 语义断言的文件**（`ui-docker-e2e.py`、`ui-unified-entry.py`、`verify-workbench-origin.mjs`、`ui-provider-degrade.py`）。其中 **`ui-docker-e2e.py` 是老「三端口」专用测试，已过时**，需人工决定重写或废弃
- **② 无用 Dockerfile / nginx**
  - **删除**根 `Dockerfile` 与根 `nginx.conf`（原「三端口」教务端镜像，已无任何编排/文档引用）
  - **保留** `ai-workbench/Dockerfile` + `ai-workbench/nginx.conf`（工作台单独部署用；且是 `运维约定` §1/§2 两条事故教训的落点）
  - 同步：`deploy/nginx-unified.conf` 注释、`docs/06-部署/运维约定.md` §1（三份→两份）、`memory/check.mjs` 清单
- ★ 顺带发现并修正：`docs/06-部署/运维约定.md` §6 的运维命令 `docker exec attendance-web nginx -s reload` —— 该容器已不存在 → 改为 `attendance-unified`；项目级 skill `feature-dev-flow` 同步（3.3 / 2.2 / 扫描命令 / ⑤）
- 提交: `0e7002e`（43 files, +443/−454）
- 推送: `git -c http.proxy= -c https.proxy= push gitee main` → `3d4f3f1..0e7002e`，远端 ref 已核实一致
- 踩坑: `git add -A` 把**先建的临时脚本** `_cm.py`/`_st2.txt` 一并提交了 → `git rm` + `--amend` 修掉
  （教训：**先 `git add` 再写临时文件**，或把临时文件放到仓库外；另：`git add -A` 前先确认根目录无 `_*` 残留）

## 10:55 | Decision | 版本策略：本地=服务器；老三端口资产全部废弃；不推 GitHub

- 来源: 会话（用户拍板，回应上一轮的两个待决项）
- 原则（K-027）:
  1. **本地 = 服务器** —— 同一份统一入口编排（`docker-compose.yml`，端口 18080），不再维护三端口形态
  2. **老三端口彻底废弃**（服务器与本地都不再用）
  3. **GitHub 不做**，只推 gitee
- 落地:
  - 废弃老三端口专用 e2e：删除 `_verify_test/ui-docker-e2e.py`（它断言跳转 URL 必须含 `:8082`，与新形态直接冲突）
  - 废弃「工作台单独部署」构建资产：删除 `ai-workbench/Dockerfile` + `ai-workbench/nginx.conf`（此前仅老三端口 compose 使用，已零引用）
  - 同步：`deploy/nginx-unified.conf` 注释（→ 唯一 nginx）、`docs/06-部署/运维约定.md` §1（一份 nginx）/§2（改写为「工作台自托管时代」的历史事故，原理保留）、`memory/check.mjs` 清单
  - **明确不废弃**（测的是**代码行为**、不是部署形态，需重写适配统一入口）：
    `_verify_test/ui-provider-degrade.py`（无 Key 降级链路）、`_verify_test/verify-workbench-origin.mjs`（跳转推导，覆盖 samesite/split 两种配置分支）
- 关联: K-027

## 11:20 | Correction | ★ 事故：`git rm` 连带删掉整个 `ai-workbench/`（已完全恢复）

- 来源: 会话（「推 gitee 前一并废弃老三端口资产」执行期间）
- **事故**：`git rm -f ai-workbench/Dockerfile ai-workbench/nginx.conf` 只报告删了 2 个文件，
  却把**整个 `ai-workbench/` 目录（34 个文件、纯前端源码）一并清空**；随后 `git add -A`
  把它当成删除提交（`584535c`，41 files / **−10930 行**）并推送到 gitee
- **排查过程**（方法论值得复用）：
  1. 怀疑 lint-staged / husky 钩子 → 查 `.git/hooks` 无非样例钩子、`core.hooksPath` 为空 → **排除**
  2. 怀疑外部程序（杀软/同步/其他会话）持续删除 → 恢复后**纯磁盘轮询 60 秒**，34 个文件纹丝不动 → **排除**
  3. **逐条 git 命令定位**：`git checkout` 安全（34→34）；**`git rm -f` → 34 → 目录消失**；`git add -A` 安全 → **锁定 `git rm`**
  4. 叠加故障：被中断的命令留下**孤儿 git 进程 + 陈旧 `.git/index.lock`**，导致后续恢复静默失败
     （`fatal: Unable to create '.git/index.lock': File exists`）→ `taskkill` 孤儿进程 + 删锁后才恢复
- **恢复方式（关键，已连续 3 次成功）——完全绕开 `git rm`**：
  1. `git checkout 011d4be -- ai-workbench`（索引 + 工作区恢复）
  2. `git update-index --force-remove <file>`（**纯索引删除，绝不碰工作区**）
  3. `git write-tree` → `git commit-tree <tree> -p <parent> -F msg` → `git update-ref refs/heads/main <commit>`
  4. `git reset --mixed HEAD` 同步索引；工作区文件用 **`os.remove`（不用 `git rm`）** 删
  5. `git push gitee main`
- 结果: `81a40e6`（恢复 32 个 ai-workbench 文件，保留原定 2 项废弃）→ `1a88860`（清掉误入库的临时脚本）
- 校验: 对 `011d4be` 的 `ai-workbench` diff = **仅 2 个文件（97 行）被删**，源码逐字恢复；
  另 9 项原定改动（白屏修复 / Docker 统一 / LLM 归页面 / 文档）全部在位
- 影响面: **仅仓库文件；生产数据与服务器完全未受影响**（数据在 `server/data/` bind mount，与 git 无关）
- 关联: K-028
