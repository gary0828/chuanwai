# CURSOR · 当前进度（L0）

> ★ 每次会话必读，每次会话**整体覆写**。只留「主线 + 阻塞 + 下一步」，细节去 `logs/` 与 `knowledge/`。
> 覆写时间：2026-09-26 21:15

## 主线位置

**又完成一批：第一批「止血」5 项**（功能做精审计而来），**已验证**（e2e 75/0 + 专项 34/0），**待提交并等你在 18080 实测**。
第二批（撤销闭环）与第三批（提效）**待你定批次后再动**。

| 项 | 状态 |
|---|---|
| ① **通用删除守卫**（courses / terms / orders） | ✅ 完成（删课程提示 12 类引用，旧实现只查 4 类、漏 9 类） |
| ② **学生删除影响预览**（删除**前**可见代价） | ✅ 完成 |
| ③ **财务净额口径**（对齐 ADR-003，改 7 处） | ✅ 完成（**"已缴"数字会变小，需你确认**） |
| ④ **考勤修改不再产生重复行** | ✅ 完成 |
| ⑤ **报班强制选课程** | ✅ 完成 |
| 订单删除改**分级**（在读拒绝 / 非在读允许） | ✅ 完成（解开"删除链"死结） |
| 横向扫描（schedules/users/exams/classes） | ✅ 现状可接受，未改 |
| 表格固定列叠字修复 | ✅ 已修（上一轮） |
| **P0 修：批量导入静默失效** | ✅ 已推 gitee `5bc2a62` |
| **业务流测试** | ✅ 报告：`docs/08-参考/业务流测试结果-2026-09-26.md` |
| 菜单重构 + 出勤率口径 + 备份 VACUUM INTO | ✅ 已推 gitee `d779d2b` |

## 请你看这几处（实测指引）

1. **学员管理 → 学生管理 → 导入**：先点「模板」下载 → 填 2 行（班级名用系统的）→ 导入
   → 应出现**导入结果弹窗**（成功 N / 失败 M）；**同一文件再导一次**也必须有弹窗（提示"学号已存在"）
2. **导入一次故意写错班级名** → 应提示「第X行（学号）班级不存在，已取消导入」
3. 导入后在列表里核对**「来源渠道」「报名日期」**两列是否真的填上了（这两列以前会丢）
4. 导入时按钮应显示 **「导入中…」** 并转圈（以前没有任何反馈）
5. **家校沟通 → 通知记录**：右侧「生成时间 / 状态 / 操作」三列应**清晰分离**，不再出现叠字

## ⚠ 待用户拍板

- ★ **新 S8（最要紧）：「功能做精」审计出 60+ 处可做精，其中 3 处属「静默改错数据」**
  → ①改一条考勤会新增重复行（统计双计/课时多扣）②删课程静默清空订单课程 → 永不扣课时
  ③财务「已缴/实收」未减退费（与自家 ADR-003 冲突）+ 缺「剩余学费」
  → 详见 `docs/08-参考/功能做精审计-2026-09-26.md`；**建议先做第一批 5 项（止血）**，等用户选批次
- **新 S7**：5 个表格页列宽仍溢出（报班管理 **+310px** 最大）→ 已不混乱，报班管理 14 列需你定删/合并哪些列
- **新**：本地库有 **21 条历史测试残留学员**（`QA*`/`PBMU*` 前缀，**会计入统计分母与首页数字**）
  → 若这个库要拿去演示/交付，建议清理；**删数据需你点头**
- **新**：学习报告「考勤 0 次」与「暂无数据」未区分（P2）
- 旧：`ROADMAP.md` §11 的 S1（4 条测试残留课次）/ S2（恢复不清 related_session_id）/ S3~S6
- 旧待办：P0 剩余五项（G3/G4/G5/G11/G7）｜ Phase 1 数据地基 ｜ Gate 3 技术债

## 原则（用户拍板）

- **K-032**：先教务系统 → 再 AI 工作台 ｜ **K-027**：本地=服务器、只推 gitee
- **K-020**：重建生产 docker 单独问用户 ｜ **K-018**：需求未澄清不动手
- **K-019**：功能做完先开环境给用户测（18080）
- **K-050（新）**：**静默即缺陷** —— 任何用户操作都要有明确反馈；"要点/要选/要传"的环节必须走真实 UI 才算测过

## ⚠ 本机硬红线（详见 `knowledge/git-local.md`）

- **禁用 `git rm`**（K-028，会清空整个目录）
- **推送**：直接 `git push gitee main`；**卡住**先 `env -u http_proxy -u https_proxy -u HTTP_PROXY -u HTTPS_PROXY` 再推（K-042）
- 中文目录 → 两步法构建 + **`up -d --force-recreate --no-build`**（K-040）+ 重建 server 后 `nginx -s reload`
- 改上传功能前**先读 K-049**（本项目 3 处 `before-upload`，`profile`/`settings` 是正确范例）
- ⚠ 工作区**恒有 1 个未跟踪文件** `学科机构_全学段教务管理平台_PRD_v1.0.docx`（**有意不入库**，属预期）
