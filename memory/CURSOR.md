# CURSOR · 当前进度（L0）

> ★ 每次会话必读，每次会话**整体覆写**。只留「主线 + 阻塞 + 下一步」，细节去 `logs/` 与 `knowledge/`。
> 覆写时间：2026-09-23 22:40

## 主线位置

**G1+G2（课次实体 + 任课关系，迁移 v21）已交付并推送 gitee** —— 远端 `main` = 本地 `0fc3ec1`。
上一轮卡住的**唯一卡点（推送凭据）已解除**，解法沉淀为 `git-local.md` **K-039**。
**下一步待用户发话**：开工 P0（G3 状态机 / G4 口径字典 / G5 修正留痕 / G11 危险操作确认 / G7 学员 360）。

| 项 | 状态 |
|---|---|
| v20 个人中心 / 自助改密码 / 头像 | ✅ 已推送 `b6d8f59` |
| C4 内容清点（35 页全量）+ 报告 | ✅ 报告：`docs/08-参考/内容清点报告-2026-09-23.md` |
| C6–C9 修复 | ✅ 已推送 `9aef4a9` |
| **G1+G2 课次实体 / 任课关系（v21）** | ✅ **已推送 `444f6e9` → `1196fd8`** |
| **本地 Docker 已重建并验证**（等你明天测） | ✅ `http://localhost:18080`（库 v21 · e2e 80/80 · 课次 91/91 · 个人中心 28+27 · C6–C9 16/16） |
| 推送凭据卡点 | ✅ **已解**（K-039：先 `credential-manager get` 取凭据 → 内联 helper 推） |
| 生产容器重建 | ⏸ 用户明确暂不动（K-020） |
| 服务器原地升级 | ⏸ 等用户给 SSH 地址 |

## 待用户拍板 / 可选下一步（选定后按 `feature-dev-flow` 走）
- ⏳ **P0 剩余五项**：G3 状态机 · G4 口径字典 · G5 修正留痕 · G11 危险操作确认 · G7 学员 360
- Phase 1 数据地基（F1 学员账户 · F2 课评 · F3 知识点）｜Gate 3 技术债（H11/H9/H5/H14）
- 小项 I1/I2（删员工清头像文件 / 头像恢复默认）｜工作台遗留 **C5**（Dashboard `followUps` 疑似演示数据泄漏）

## 原则（用户拍板）
- **K-032**：先教务系统 → 再 AI 工作台
- **K-027**：本地 = 服务器（同一份统一入口编排、18080）｜只推 gitee
- **K-020**：重建生产 docker 单独问用户

## ⚠ 本机硬红线（详见 `knowledge/git-local.md`）
- **禁用 `git rm`**（K-028，会清空整个目录）→ `git update-index --force-remove` + `os.remove`
- **推送凭据**：非交互会话 GCM 会卡死/失败 → 用 **K-039** 的姿势；且必须 `-c http.proxy=`（死代理 K-024）
- 中文目录 → `docker compose --build` 必失败，走两步法 + **`up -d --force-recreate --no-build`**
  （★ 少了 `--force-recreate` 容器不会重建，会继续跑旧镜像 —— **K-040**）
- 跑脚本 / 构建的环境事实 **K-035**；清点方法 **K-037**；模板插值坑 **K-038**
- ⚠ 工作区**恒有 1 个未跟踪文件** `学科机构_全学段教务管理平台_PRD_v1.0.docx`（**有意不入库**，属预期）
