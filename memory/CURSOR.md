# CURSOR · 当前进度（L0）

> ★ 每次会话必读，每次会话**整体覆写**。只留「主线 + 阻塞 + 下一步」，细节去 `logs/` 与 `knowledge/`。
> 覆写时间：2026-09-23 14:15

## 主线位置

**方向已改（K-032）：暂停 AI 工作台，先补教务系统 P1 硬伤。** 本轮 **C3 自助改密码 + C2 员工头像**代码/文档/验证已全绿（v20 已应用、生产镜像已重建、18080 可访问）→ **等你浏览器实测通过**，通过后才推送 gitee。

| 项 | 状态 |
|---|---|
| 需求澄清 + 横向同类扫描 + 《开发计划》 | ✅ 归档 `docs/07-架构与决策/ROADMAP.md` §七之二 |
| 迁移 **v20** `users.avatar` + 3 个自助端点 + 公共 `utils/image.js` | ✅ 已落地 |
| 个人中心页 / 顶栏下拉入口 / 员工管理页头像列 | ✅ 已落地 |
| **用户实测（门禁 3）** | ⏸ **等你** |
| 推送 gitee | ⏸ 实测通过后 |
| 服务器原地升级 | ⏸ 本轮本地验完再一次性同步（**缺 SSH 地址**） |

## 原则（用户拍板）
- **K-032**：先教务系统（P1 硬伤）→ 再 AI 工作台；AI 工作台那 4 个澄清问题已作废
- **K-027**：本地 = 服务器（同一份统一入口编排、18080）｜老三端口彻底废弃｜只推 gitee
- **K-020**：重建生产 docker 单独问用户

## ⚠ 本机硬红线（详见 `knowledge/git-local.md`）
- **禁用 `git rm`**（K-028，会清空整个目录）→ 用 `git update-index --force-remove` + `os.remove`
- 命令被中断 → 先杀孤儿 git 进程 + 删 `.git/index.lock`
- 推送必须 `-c http.proxy=` + 清 `HTTP(S)_PROXY`
- 中文目录 → `docker compose --build` 必失败，走两步法 + `up -d --no-build` → 再 `nginx -s reload`

## 下一步
1. **等你实测**（`http://localhost:18080`；顶栏右上角下拉 →「个人中心」；admin 与 teacher 各测一遍改资料 / 上传头像 / 改密码填错原密码应被拒）
2. 通过后：`code-review-and-quality` 五轴审查 → 验收清单 → 推送 gitee
3. 再谈：是否做 I1/I2（删员工清头像文件 / 头像恢复默认）· 是否开 C4 全系统逐界面走查 · 服务器原地升级
