# CURSOR · 当前进度（L0）

> ★ 每次会话必读，每次会话**整体覆写**。只留「主线 + 阻塞 + 下一步」，细节去 `logs/` 与 `knowledge/`。
> 覆写时间：2026-09-26 16:20

## 主线位置

**菜单重构（方案 A）+ 出勤率口径统一 + 备份实现统一 已完成开发并开好环境** —— 本地 `http://localhost:18080`
镜像已重建取证、全套验证通过（菜单 51/51 + UI 32/32 · e2e 80/80 · 课次 90/91 · openapi exit 0）。

**⛔ 卡在门禁 3：等你实测。实测通过后再谈提交与推送。**

| 项 | 状态 |
|---|---|
| **菜单重构**（admin 9 组 / teacher 6 组，**子项 path 全未改**） | ✅ 已开环境待测 |
| **出勤率口径统一**（`utils/attendance-rate.js`，收口 9 处） | ✅ 已验证（20301 组对拍一致） |
| **备份改 `VACUUM INTO`**（与运维脚本同一实现） | ✅ 已验证（integrity ok/外键 0 违规） |
| 课程页「默认授课教师」标注 | ✅ 已开环境待测 |
| 修正上一轮**误报**（课消统计"权限矛盾"实为误判） | ✅ 已更正报告与 PROGRESS |
| 顺手修复回归脚本「自污染→崩溃」（K-047） | ✅ 课次回归恢复可跑 |
| **v22 改名工作**（「调课」→「挪课」等） | ⏸ **仍未提交**（09-24 起的门禁 3 未走完） |
| 生产容器重建 / 服务器升级 | ⏸ 用户明确暂不动（K-020） |

## ⚠ 工作区有两批**未提交**改动（推送时必须分清）

1. **v22 改名工作**（09-24 起，等实测）：`022-session-reschedule-rename.js`、`sessions.js`、`schedules.js`、`auth.js`（部分）等
2. **本次（09-26）菜单与口径**：`auth.js`、`attendance.js`、`analytics.js`、`reports.js`、`agent.js`、
   `utils/backup.js`、`utils/attendance-rate.js`(新)、`verify-sessions.mjs`、`courses/index.vue` + 文档

> 两批都在 `auth.js` 上，`git add` 时**无法按文件区分** → 建议**一起提交**，或先让用户确认 v22 改名也通过。

## 请你看这几处（实测指引）

1. **侧边栏**：`考勤管理`（4 项）/ `排课与课表`（6 项）/ `学员管理` / `教学成果` / `招生与报名` / `财务` /
   `通知记录` / `系统管理`（6 项）—— ⚠️「家校沟通」只 1 个子项，框架会**提升**为顶层「通知记录」，**不是 bug**（K-048）
2. **统计报表 vs 经营概览**：同一区间的出勤率现在应**完全一致**（原先差 0.6 个百分点）
3. **系统管理 → 数据备份**：点一次「备份」，能用即 OK（已改用 VACUUM INTO）
4. **学员管理 → 课程管理**：字段已叫「默认授课教师」，页内写明实际任课去「任课关系」
5. **教师账号登录**：应只看到 5 组，**看不到**财务 / 招生与报名 / 系统管理

## 待用户拍板（详见 `docs/07-架构与决策/ROADMAP.md` §11）

- **S1** 4 条历史测试残留的「挪课」课次（`class_id=1`，2026-11-30~12-03 第 8 节）是否清理
- **S2** 「挪课→停课→恢复」不清 `related_session_id` → 该课次永久无法再挪（**语义待定**）
- S3 `analytics.js` 死变量 `range` ｜ S4 `server/` 全目录既有 prettier 报错 ｜ S5/S6 可选重构
- 旧待办：P0 剩余五项（G3/G4/G5/G11/G7）｜ Phase 1 数据地基 ｜ Gate 3 技术债

## 原则（用户拍板）

- **K-032**：先教务系统 → 再 AI 工作台
- **K-027**：本地 = 服务器（同一份统一入口编排、18080）｜只推 gitee
- **K-020**：重建生产 docker 单独问用户
- **K-018**：需求未澄清不动手 ｜ **K-019**：功能做完先开环境给用户测（18080）

## ⚠ 本机硬红线（详见 `knowledge/git-local.md`）

- **禁用 `git rm`**（K-028，会清空整个目录）→ `git update-index --force-remove` + `os.remove`
- **推送**：直接 `git push gitee main` 即可（git 配置死代理已删）。**卡住**先
  `env -u http_proxy -u https_proxy -u HTTP_PROXY -u HTTPS_PROXY` 再推（K-042，`ls-remote` 通 ≠ `push` 通）
- 中文目录 → 两步法构建 + **`up -d --force-recreate --no-build`**（少了它不换镜像，K-040）
- 重建 `server` 后**必须** `docker exec attendance-unified nginx -s reload`，否则 `/api/*` 502
- 跑脚本环境事实 **K-035** ｜ 清点方法 **K-037** ｜ 模板插值坑 **K-038** ｜ 脚本自污染 **K-047**
- ⚠ 工作区**恒有 1 个未跟踪文件** `学科机构_全学段教务管理平台_PRD_v1.0.docx`（**有意不入库**，属预期）
