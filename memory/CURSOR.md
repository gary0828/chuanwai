# CURSOR · 当前进度（L0）

> ★ 每次会话必读，每次会话**整体覆写**。只留「主线 + 阻塞 + 下一步」，细节去 `logs/` 与 `knowledge/`。
> 覆写时间：2026-09-23 15:10

## 主线位置

**上一步已完结**：C3 自助改密码 + C2 员工头像（v20）已推送 gitee `b6d8f59`。
**当前停在**：**C4 全系统逐界面内容清点已完成**（35 页全量），报告在 `docs/08-参考/内容清点报告-2026-09-23.md`；
**等用户指定修哪些**（待决策 C6–C9，见下）。**只列清单、未改任何代码。**

| 项 | 状态 |
|---|---|
| v20 个人中心 / 自助改密码 / 头像 | ✅ 已推送 `b6d8f59` |
| **C4 内容清点（35 页）** | ✅ 完成 → 报告 + ROADMAP §6 已更新 |
| 清点结论 | A 类（写死演示数据）**未发现** · C 类（越界外链）**未发现** · B 类 1 处死字段 · D 类含 **1 处用户可见缺陷（C6）** |
| 按 C6–C9 修不修 | ⏸ **等用户拍板** |
| 生产容器重建 | ⏸ 用户明确暂不动（K-020） |
| 服务器原地升级 | ⏸ 等用户给 SSH 地址 |

## 待用户拍板（明细见 ROADMAP §6「C4 走查产出的待决策」）
- **C6** 统计页空态文案字面显示 `{{ }}`（`attendance/statistics/index.vue:596,645`）—— **建议立即修**
- **C7** 铃铛 `avatar` 死字段 · **C8** 登录页占位入口（需产品决策）· **C9** Dify 占位分组 + `index.html` 模板标题
- 另：I1/I2（删员工清头像文件 / 头像恢复默认）· 下一大块（Phase 1 数据地基 / Gate 3 技术债）

## 原则（用户拍板）
- **K-032**：先教务系统（P1 硬伤）→ 再 AI 工作台；AI 工作台那 4 个澄清问题已作废
- **K-027**：本地 = 服务器（同一份统一入口编排、18080）｜只推 gitee
- **K-020**：重建生产 docker 单独问用户

## ⚠ 本机硬红线（详见 `knowledge/git-local.md`）
- **禁用 `git rm`**（K-028，会清空整个目录）→ `git update-index --force-remove` + `os.remove`
- 推送必须 `-c http.proxy=` + 清 `HTTP(S)_PROXY`（本机有失效死代理）
- 中文目录 → `docker compose --build` 必失败，走两步法 + `up -d --no-build`
- 跑脚本的环境事实见 `knowledge/process.md` **K-035**；内容清点方法见 **K-037**

## 下一步
1. 等你指定修 C6–C9 中的哪些（建议至少 C6）
2. 或直接指定下一大块：Phase 1 数据地基（学员账户 / 课评 / 知识点）或 Gate 3 技术债
