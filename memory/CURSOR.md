# CURSOR · 当前进度（L0）

> ★ 每次会话必读，每次会话**整体覆写**。只留「主线 + 阻塞 + 下一步」，细节去 `logs/` 与 `knowledge/`。
> 覆写时间：2026-09-23 15:40

## 主线位置

**内容清点这一轮已闭环**：C4 全量走查（35 页）→ C6–C9 全部修复 → 已推送 gitee `9aef4a9`。
**当前主线**：**G1+G2（课次实体 + 任课关系）开发完成、环境已就绪，⏳ 等你在浏览器实测（门禁③）**。
团队 `software-jiaowu`（PM/架构师/工程师/QA）走完 SOP；QA 判定 **NoOne（无源码缺陷）**：专项 100/100 · e2e 80/80 · analytics 22/22 · check-openapi exit 0 · 浏览器双角色 42/42 · `pnpm build` exit 0。**未 commit、未 push、未动生产容器。**
**测试环境**：本地统一入口已重建（含 v21 代码）→ `http://localhost:18080` ｜ 本地库已升 **v21**，**测试前已备份** `server/data/backups/local-pre-v21-20260923-204933.db`（+ wal/shm）｜ 抽查：`/` 200、登录 OK、teacher 对 leads/finance 均 403。
**本轮新增产出**：**商用化蓝图 v2.2**（`docs/08-参考/教务功能补全分析-2026-09-23.md`）· `PRD借鉴分析` · **ADR-009~012** · `ROADMAP` **§5.2（G 系列）+ §七之四（G1+G2）** · 迁移 **v21** · `server/docs/021-*-PRD.md` 与 `-设计.md` · 测试脚本 `server/scripts/verify-sessions.mjs` + `_verify_test/ui-sessions-verify.py`。
**下一步范围待用户拍板**（见下）。生产容器**未动**（用户明确暂不动）。

| 项 | 状态 |
|---|---|
| v20 个人中心 / 自助改密码 / 头像 | ✅ 已推送 `b6d8f59` |
| C4 内容清点（35 页全量）+ 报告 | ✅ 报告：`docs/08-参考/内容清点报告-2026-09-23.md` |
| **C6–C9 修复** | ✅ 已推送 `9aef4a9`（验证 16/16，回归 e2e 80/80 + profile 28/28） |
| 生产容器重建 | ⏸ 用户明确暂不动（K-020） |
| 服务器原地升级 | ⏸ 等用户给 SSH 地址 |

## 待用户拍板 / 可选下一步
- ✅ **D1–D6 已全部拍板** → 已落地 ADR-009~012 + ROADMAP §5.2/§七之三
- ⏳ **下一步待你发话**：开工 **P0 七项**（G1 课次实体 · G2 任课关系 · G3 状态机 · G4 口径字典 · G5 修正留痕 · G11 危险操作确认 · G7 学员 360）
  → 按 `feature-dev-flow` 五步门禁走（**未动代码、未推 git**）｜★ **G1 必须先做**（F4–F7 与作业模块都在等它）
- **Phase 1 数据地基**：F1 学员账户（实缴/已扣/剩余学费 + 全校预收账款）· F2 课堂评价 · F3 知识点体系（~10 人日）
- **Gate 3 技术债**：H11 缺索引 · H9 明文默认口令 · H5 逻辑删除未过滤 · H14 `xlsx` CVE
- 小项 **I1/I2**（删员工清头像文件 / 头像恢复默认，见 `server/docs/020-*` 改进项）
- 工作台侧遗留 **C5**（Dashboard `followUps` 疑似演示数据泄漏，未核实）

## 原则（用户拍板）
- **K-032**：先教务系统 → 再 AI 工作台；AI 工作台那 4 个澄清问题已作废
- **K-027**：本地 = 服务器（同一份统一入口编排、18080）｜只推 gitee
- **K-020**：重建生产 docker 单独问用户

## ⚠ 本机硬红线（详见 `knowledge/git-local.md`）
- **禁用 `git rm`**（K-028，会清空整个目录）→ `git update-index --force-remove` + `os.remove`
- 推送必须 `-c http.proxy=` + 清 `HTTP(S)_PROXY`（本机有失效死代理）
- 中文目录 → `docker compose --build` 必失败，走两步法 + `up -d --no-build`
- 跑脚本 / 构建的环境事实见 `knowledge/process.md` **K-035**；清点方法 **K-037**；模板插值坑 **K-038**

## 下一步
1. 等你选上面任一方向（或提新需求）
2. 选定后按 `feature-dev-flow` 走：需求澄清 → 扫描与计划 → 开发 → 开 18080 实测 → 推送
