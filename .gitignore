node_modules
.DS_Store
dist
dist-ssr
*.local
.eslintcache
report.html
vite.config.*.timestamp*

yarn.lock
npm-debug.log*
.pnpm-error.log*
.pnpm-debug.log
tests/**/coverage/

# ============ 项目自定义忽略 ============

# ── 环境变量 ──────────────────────────────────────────────────────
# 只忽略「真密钥 / 本地覆盖」，放行 pure-admin 的构建期 env（仅含 VITE_*，无密钥）：
#   · 忽略：.env（含 JWT_SECRET / LLM_API_KEY 等真密钥）、以及任意 *.local 本地覆盖
#   · 放行：.env.example / .env.development / .env.production / .env.staging
# ★ 2026-09-23 生产白屏事故：原 `.env.*` 一刀切把 .env.production 也忽略了 →
#   git clone 部署时构建机拿不到 VITE_ROUTER_HISTORY → 前端启动即崩、整页白屏。
.env
.env.local
.env.*.local
!.env.example
!.env.development
!.env.production
!.env.staging

# 浏览器验证截图（可能含真实学员姓名 / 家长电话 / 金额）
evidence/

# Obsidian 工作区配置（插件缓存、本地视图状态）
.obsidian/

# 本地临时验证目录
_verify_test/

# WorkBuddy 会话数据与 AI 长期记忆（本机会话日志，含本机绝对路径）
.workbuddy/

# 日志文件
*.log

# 内部计划/临时文档（不用提交，完成后删除）
.trae/*
# 项目流程 skill（data-model-review）纳入版本控制，其余 .trae 内容保持忽略
!.trae/skills/
.trae/skills/*
!.trae/skills/data-model-review/
# 本地学习经验（self-improving-agent 记录，不入库）
.learnings/
# SQLite 数据文件与备份目录（种子数据由 seed.js 首次启动生成）
server/data/

# Editor directories and files
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
tsconfig.tsbuildinfo

# ============ AI 教学工作台（ai-workbench/）============
# 构建产物与依赖（与根目录同规则）
ai-workbench/dist/
ai-workbench/node_modules/

# ============ Dify（dify/）============
# 运行时数据卷：PostgreSQL / Redis / 向量库 / 上传文件，体量大且含运行时数据
dify/volumes/
# 上游源码临时克隆目录（仅用于取 docker 配置，用完即删）
.dify-src/
