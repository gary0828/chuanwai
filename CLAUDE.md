# CLAUDE.md

> 本项目的**会话启动入口**是 `memory/INDEX.md`。**先读它，再动手。**
> （与 `AGENTS.md` 同一份约定，只是文件名不同，供不同工具各自识别。）

## 三步启动

1. 读 `memory/INDEX.md`（唯一入口，含项目一屏介绍与指针表）
2. 读 `memory/CURSOR.md`（当前进度：在做什么 / 卡在哪 / 下一步）
3. 按 INDEX §3 的指针**按需**读详情——**未命中就停手，不要顺手读别的**

## 收尾

按 `memory/PROTOCOL.md` §2 的五条清单归档，并跑 `node memory/check.mjs`。

## 禁止

- 启动时整读 `README.md` / `docs/08-参考/PROGRESS.md` / `WORKBUDDY.md` 全文
- 往 `memory/INDEX.md` 里堆正文
- 改昨天的日志（要修正就追加 `Correction:`）
