# Impeccable 设计上下文 · 教学管理系统

> 本文件由 Impeccable 工作流生成并持久化。**任何前端设计 / 改版 / 样式工作开始前必须先读本文件。**
> 视觉规范的具体数值以 `src/style/tokens.scss` 为唯一权威源；本文件负责说明**为什么**这样定。

---

## Design Context

### Users

培训机构**员工端 CRM**（学生与家长无账号、不登录）。单校区，学员 200–500 / 员工 5–15。两类角色：

- **admin（校长 / 教务主管）** — 1–2 人。办公室台式机；关心"这个月招了多少人、收了多少费、课消了多少、哪个班快满了"。看的是报表，需要一眼抓住趋势与异常。
- **teacher（任课 / 班主任）** — 5–13 人。教室与办公室，**台式机 + 平板并用**。高频动作是"打开班级 → 点名签到 → 记请假"，常在课间几分钟内完成；金额对其脱敏。

关键情境约束：**课上操作、时间碎片化、单手触控**。这决定了平板是一等公民，触控热区与表格降级是硬需求而非加分项。

### Brand Personality

**可信赖 · 精确 · 不打扰**

情绪目标：老师点开系统时感到"这是个靠得住的工具"，而不是"又要面对一个复杂后台"。界面应退到数据后面——存在感来自秩序与准确，而非装饰。

**反参考（明确不要）**：
- ❌ 通用 Element / Ant Design 出厂皮肤（默认蓝 `#409EFF`、糖果色标签、圆角 + 通用阴影的卡片）
- ❌ AI 模板特征：青紫渐变、发光边框、毛玻璃堆叠、无意义火花线、用等宽字体硬凑"技术感"
- ❌ 大数据屏式深色霓虹、英雄指标大数字铺满首屏
- ❌ 大圆角（12px+）、卡片套卡片

### Aesthetic Direction

**「教务台」** —— 参考纸质成绩单与教务档案的秩序感：结构清晰、边界精确、数字成列对齐、没有一处多余装饰。

| 维度 | 决策 |
| --- | --- |
| 主题 | 浅色为主（`OverallStyle: light`），深色模式完整适配但非主场景 |
| 主色 | 深靛蓝 `#1F5C99`（比默认蓝更沉、饱和更低 → 机构信赖感而非科技感） |
| 中性 | 冷灰阶，**向品牌色微偏**（纯灰在浅底上显廉价） |
| 语义色 | 去糖果化：成功 `#2E7D5B` / 警告 `#B7791F` / 危险 `#B3453C` |
| 字体 | 中文系统字体栈，**不引入 Web 字体**（中文子集动辄数 MB 不划算）；用 `tabular-nums` 实现数字对齐 |
| 圆角 | 克制的 3–10px 阶梯，弹窗与卡片不用大圆角 |
| 阴影 | 极克制双层微阴影；边界优先靠细边框与留白 |
| 动效 | 只动 `opacity` / `transform` / 颜色；`ease-out` 类缓动；120–240ms |

**差异化的那一个点**：让「看数字」本身成为视觉特征 —— 表格只有横线无竖线、表头与金额逐位对齐、课时与统计口径用等宽数字呈现、分隔线精确到 1px。用户应该感觉「这个系统对数字很认真」，而不是「这个系统很好看」。

- 登录页背景 = 「教务格栅」：全屏对称的两级坐标格栅（主格 128px / 次格 32px），把全站「只有横线的表格」这一线条语言抽象到背景层，中心径向渐隐给卡片留出呼吸区

### Design Principles

1. **功能零变更** — 视觉改版不得触碰任何 API 调用、权限逻辑、业务口径与交互流程。改版只动表现层。
2. **密度服务于扫读** — 教务场景以"找一条记录、核对一个数字"为主。宁可信息更密，也不要为了留白把一屏内容推到两屏。
3. **边界靠分隔线，不靠阴影** — 卡片用细边框 + 无阴影；表格只用横向分隔线。阴影只留给真正浮起的层级（弹窗、下拉、气泡）。
4. **token 是唯一真相** — 所有颜色 / 间距 / 圆角 / 阴影 / 动效一律走 `tokens.scss`。页面里出现硬编码色值即视为缺陷。
5. **平板是一等公民** — 触控热区 ≥40px、表格在窄屏可降级、弹窗宽度不得溢出。老师会在课上用平板操作。
6. **背景与装饰从既有语言抽象，不发明新语言** — 任何装饰（背景纹理、空状态、分隔线、语义色条）都应能追溯到全站已存在的视觉语言。判断标准：能不能说出它对应系统的哪一处既有表达 —— 说不出就不用。渐变光晕、霓虹、插画在本项目站不住，正因它们是「外来语」。

### 反模式清单（每次改版逐条自查）

- 硬编码颜色 `#xxxxxx` 或 Element 出厂色值（`#409EFF` / `#67C23A` / `#E6A23C` / `#F56C6C`）
- 表格同时使用 `border` + `stripe`（边框与斑马纹叠加 = 最大视觉噪音源）
- 弹窗写死 `width="480px"`（窄屏溢出）
- 用 `<component :is="'ep:xxx'" />` 渲染图标（项目规范禁止，且实际不渲染）
- 页面内直接使用 `axios` / `$router` / `$route` / `localStorage`（项目规范禁止，违者破坏分层）

---

## 项目硬约束（优先级高于本文件的一切美学建议）

本项目有自己的工程门禁，与 Impeccable 的通用建议冲突时**项目门禁优先**：

- 遵循 pure-admin 官方规范；接口集中在 `src/api/*.ts`；图标用 `~icons/ep/xxx`
- DB 变更必须加迁移 + 同步 `server/database.md`；API 变更必须同步 `docs/api.md` + `docs/openapi.yaml`
- 每个功能改动都要浏览器验证（Playwright，截图入 `evidence/`）
- 权限四层同时生效：菜单不下发 / 接口 403 / 字段脱敏 / 导出不含费用
- 上线后的功能回归基线：`e2e-lifecycle.mjs`（80 项）+ `analytics-smoke.mjs`（22 项）+ `ui-p0-verify.py`（31 项）

---

生成时间：2026-09-18
