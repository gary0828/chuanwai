# 产品路线图（ROADMAP）

> 生成日期：**2026-09-12** ｜ 依据：深度需求访谈（12 个决策点全部收敛）
> 适用范围：**单校区自用**培训机构员工端 CRM ｜ 规模：**在校学员 200–500 / 员工 5–15**
> 本文件与 `PROGRESS.md` 分工：`PROGRESS.md` 记录**已完成的事实**，本文件记录**待做的计划**。每阶段完成后把结论回填 `PROGRESS.md`。

---

## 〇、决策基线（已拍板，改动需重新评估下游）

| # | 决策项 | 结论 | 下游影响 |
| --- | --- | --- | --- |
| D1 | 服务范围 | **单校区自用** | 砍掉多校区、租户隔离、跨校区汇总、转校；不为这些预留架构 |
| D2 | 家校边界 | **不做家长端，不做任何出网触达** | 通知模块降级为「内部留痕」；短信/微信模板/推送全部不做 |
| D3 | 优先级方法 | **痛点驱动 + RICE 打分** | 允许主动放弃竞品标配（见 §二 Non-goals） |
| D4 | AI 实现方式 | **混合：本地算结构化指标 + DeepSeek 写自然语言** | 数字不出本地逻辑，杜绝幻觉；只把脱敏指标送模型 |
| D5 | 网络与模型 | **内网可出公网，调 DeepSeek 官方 API**，数据本地保存 | 必须做字段白名单脱敏；未配置 Key 时自动降级规则引擎版 |
| D6 | AI 触发方式 | **应用内一键生成**，结果落库可回看 / 可打印 | 不需要任务调度；生成记录要能审计（谁、何时、用了哪个模型） |
| D7 | 知识库 / 题库 | **本期只做最小依赖：知识点体系**（学科→知识点树 + 成绩知识点标签） | 不做组卷、在线答题、阅卷、错题本、RAG 知识库 |
| D8 | 财务边界 | **只做「学员账户」**：交了多少学费、扣了多少课时、剩余多少学费 | 不做提成、发票、优惠折扣、日结、分期、线上支付 |
| D9 | 排课边界 | **内部全套**：教室冲突 + 代课 + 批量排课 + 停课日历 | 不触达家长；「家长不知情」痛点本期不解决（已知取舍） |
| D10 | 招生边界 | **试听预约 + 线索公海分配 + 跟进提醒** | 不含转介绍返利、活动管理、渠道 ROI |
| D11 | 投入节奏 | **全职推进，按周冲刺** | 每阶段结束必须验收，不跨阶段并行 |
| D12 | 上线时间 | **下周 go-live**，Phase 0 只做稳定与数据准备，**不开发新功能** | 新功能从 Phase 1 起，go-live 后启动 |

---

## 一、现状判断

**强项（护城河，不要动）**：课时包联动扣减与回补、缺勤→通知→撤销的幂等链路、课消收入确认分摊、调课审批自动同步课表、线索一键转化（建档+报班事务）、成长档案全生命周期时间线。这套**业务联动完整度**超过多数收费 SaaS。

**整体覆盖度约 53%**：强在「教务执行闭环」，弱在「触达 / 经营 / 移动化」。

**最大结构性缺口**：系统在库里**没有「学生日常表现」数据**。现有可分析数据只有考勤状态、考试成绩、课时消耗、缴费记录——AI 学情分析若无课评数据，只能基于三次考试做线性回归，产出正确的废话。**因此「课堂评价」被 AI 需求倒逼为前置模块。**

---

## 二、Non-goals（明确不做，防止范围蔓延）

> 以下为**本期明确排除项**。任何一项要启用，必须回到 §〇 修改对应决策并重新排期。

**架构层面**
- 多校区 / 租户隔离 / 跨校区报表 / 学员转校
- SaaS 化、对外销售、计费与订阅
- 自定义字段、自定义角色权限（admin / teacher 两角色够用）

**家校层面**
- 家长端（小程序 / H5 / 公众号）
- 家长账号体系与家校绑定关系（v14 已删除，不回摆）
- 短信 / 微信模板消息 / App 推送等任何出网触达通道
- 家长回执、家校互评、在线投诉

**财务层面**
- 员工提成、业绩归属、课时工资、绩效工资
- 收据 / 发票打印、电子发票
- 优惠审批、优惠券、团报折扣、分期收款
- 线上支付（微信 / 支付宝 / 聚合支付）
- 日结对账单、会计凭证、总账

**教学层面**
- 完整题库（组卷、在线答题、自动阅卷、错题本）
- RAG 知识库（文档解析、向量库、embedding 检索）
- 作业布置与提交批改
- 直播课 / 双师课堂 / 录播

**其他**
- 考勤硬件（刷卡机、人脸机、二维码签到大屏）
- 移动端 App / 小程序（教师端）
- 进销存（教材教具）、加盟商管理

---

## 三、优先级方法

采用 **RICE = Reach × Impact × Confidence ÷ Effort**

- **Reach**：月均关键动作次数（系统自动执行或人工操作的次数，估算值，需业务校准）
- **Impact**：3 = 巨大（影响收入或流失）/ 2 = 高 / 1 = 中 / 0.5 = 低
- **Confidence**：对 Reach 与 Impact 估计的把握（0.5 / 0.8 / 0.9）
- **Effort**：人日（含实现 + 联调 + 浏览器验证 + 文档）

**排序例外**：标注为「前置依赖」的项不参与 RICE 排序，按依赖顺序强制前置（RICE 会系统性低估地基类功能）。

---

## 四、功能池与打分

| ID | 功能 | R（次/月） | I | C | E（人日） | RICE | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| F2 | **课堂评价（课评）** | 960 | 3 | 0.8 | 3.5 | **658** | AI 燃料 + 家长最在乎的信息；必须做「批量 + 只标异常」 |
| F12 | **学员学情报告（AI 生成）** | 500 | 3 | 0.7 | 3 | **350** | 依赖 F10 / F3 / F15 |
| F9 | **线索公海 + 分配 + 跟进提醒 + 超时回收** | 600 | 2 | 0.8 | 5 | **192** | 直接治「线索跟丢」 |
| F1 | **学员账户（实缴 / 已扣课时 / 剩余学费）** | 200 | 3 | 0.9 | 4 | **135** | 含全校「剩余学费总额（预收账款）」指标 |
| F13 | 班级学情概览 | 80 | 2 | 0.8 | 2 | **64** | 依赖 F10 |
| F11 | 预警推送（主动找人） | 30 | 3 | 0.9 | 2 | **40.5** | 依赖 F10；RICE 低估其战略价值，人工提级 |
| F8 | 试听 / 体验课预约 + 反馈 + 转化率 | 40 | 3 | 0.8 | 5 | **19.2** | 教培转化核心环节，当前完全缺失 |
| F6 | 教室 / 场地资源与冲突检测 | 40 | 2 | 0.8 | 3 | **21.3** | 依赖 D9 |
| F4 | 代课 / 换老师 | 20 | 2 | 0.9 | 2 | **18** | 依赖 D9 |
| F5 | 节假日停课日历 + 批量停课 | 8 | 2 | 0.9 | 2 | **7.2** | 依赖 D9；频次低但每次影响面广 |
| F14 | 校长问数（预置指标语义版） | 20 | 2 | 0.5 | 3 | **6.7** | **降级方案**：不做 text2sql，只允许命中已有 12 个指标 |
| F7 | 整学期批量排课 | 2 | 2 | 0.9 | 2 | **1.8** | 每学期仅用 1–2 次，但单次省数小时 |
| F3 | 知识点体系最小集 | — | — | — | 2.5 | **前置依赖** | F12 / F13 的燃料，越早建越早积累数据 |
| F10 | 本地结构化指标引擎 | — | — | — | 4 | **前置依赖** | 出勤率 / 成绩趋势 / 知识点掌握度 / 离群检测 |
| F15 | LLM provider 抽象 + 脱敏 + 降级 | — | — | — | 2 | **前置依赖** | 可配置 base_url / model / key；未配置则走规则引擎版 |

### 关于「校长自然语言问数」的风险提示

D6 选择了「校长经营洞察」，但**通用 text2sql 会让大模型写错 SQL 或理解错口径**，产出一个看起来专业、实际错误的数字——校长拿它做决策会出事。因此本期**降级为「预置指标语义问答」**：把用户输入映射到 `/api/analytics/metrics` 已定义的 12 个指标（语义匹配 + 槽位填充），**不允许生成任意 SQL**，命中不了就明确回答「这个问题我答不了」并列出可问清单。

---

## 五、路线图（按周）

### Phase 0 · Go-live 保障（约 5 人日，go-live 前完成）

> **本阶段不开发任何新功能。** 下周学员就要用，这一周只做「别出事」。

| 项 | 内容 | 人日 |
| --- | --- | --- |
| 0.1 | 补齐 P0 全链路浏览器验证：成绩（建考→录分→成绩单→通知）、销售（建线索→跟进→转化→渠道统计）、学生（新增→编辑→导入含错误行→导出）、签到联动（缺勤→通知生成 + 课时扣减） | 2 |
| 0.2 | `git init` + 首次提交 + `.gitignore` + commitlint 生效（当前仓库无 `.git`，是交付风险） | 0.5 |
| 0.3 | 补 `.env.example`；浏览器标签页标题改为系统名（当前仍是 `pure-admin-thin`） | 0.25 |
| 0.4 | 生产数据初始化：真实班级 / 学员 / 课程 / 课表 / 员工账号导入演练 | 1 |
| 0.5 | **备份恢复真机演练**（恢复一次并校验数据，不是只看备份存在） | 0.5 |
| 0.6 | 员工培训一页纸操作卡（签到 / 请假 / 报班缴费 / 成绩录入 四条主路径） | 0.5 |
| 0.7 | 上线首周现场值守与热修窗口预留 | 1 |

### Phase 1 · 数据地基（第 1–2 周，10 人日）

| 项 | 内容 | 人日 |
| --- | --- | --- |
| 1.1 | **F1 学员账户**：实缴 / 已扣课时 / 剩余学费，多订单分列汇总；学生档案新增「账户」页签；财务统计与首页新增「全校剩余学费总额（预收账款）」 | 4 |
| 1.2 | **F2 课堂评价**：课次批量评价（全班默认良好 + 只标异常）+ 单学生详评；维度见 §六 | 3.5 |
| 1.3 | **F3 知识点体系最小集**：学科 → 知识点树；考试 / 成绩录入时打知识点标签；历史成绩可补录 | 2.5 |

> **加速选项**：若希望 AI 更早见效，可在 Phase 1 末插入一个「简版学情报告」（仅基于成绩 + 考勤，1.5 人日），Phase 4 再用课评与知识点数据补全。

### Phase 2 · 排课治理（第 3–4 周，10 人日）

| 项 | 内容 | 人日 |
| --- | --- | --- |
| 2.1 | **F5 停课日历**：节假日 / 假期批量停课，自动跳过受影响课次并生成补课待办 | 2 |
| 2.2 | **F4 代课 / 换老师**：原教师 → 代课教师，课表与课消归属同步 | 2 |
| 2.3 | **F6 教室 / 场地**：教室资源 CRUD + 排课冲突检测（班级 / 教师 / 教室 三重） | 3 |
| 2.4 | **F7 整学期批量排课**：按周复制 / 按模板生成整学期课表 | 2 |
| 2.5 | 阶段回归与缓冲 | 1 |

### Phase 3 · 招生漏斗（第 5–6 周，10 人日）

| 项 | 内容 | 人日 |
| --- | --- | --- |
| 3.1 | **F8 试听 / 体验课**：试听预约排期、到课登记、试听后评价打分、试听→报名转化率统计 | 5 |
| 3.2 | **F9 线索运营**：公海池 + 分配规则 + 下次跟进时间提醒 + 我的待办 + 超时自动回收 | 5 |

### Phase 4 · 学情分析（第 7–9 周，15 人日）

| 项 | 内容 | 人日 |
| --- | --- | --- |
| 4.1 | **F15 LLM provider 抽象层**：`base_url` / `model` / `api_key` 环境变量可配；字段白名单脱敏；未配置自动降级规则引擎版；生成记录审计 | 2 |
| 4.2 | **F10 本地指标引擎**：出勤率、成绩趋势与波动、知识点掌握度、离群检测（同班 Z-score） | 4 |
| 4.3 | **F11 预警推送**：连续缺勤 + 成绩下滑 + 课时将尽 三类规则，站内推送 + 班主任待办 | 2 |
| 4.4 | **F12 学员学情报告**：结构化指标 + 图表 + DeepSeek 生成自然语言解读与建议，落库可回看、可打印 | 3 |
| 4.5 | **F13 班级学情概览**：谁在掉队 / 谁进步快 / 班级薄弱知识点，一页看完 | 2 |
| 4.6 | 全量回归（e2e + 浏览器 + Docker）与验收 | 2 |

### Phase 5 · 深化与工程治理（第 10–12 周，约 16 人日）

| 项 | 内容 | 人日 |
| --- | --- | --- |
| 5.1 | **F14 校长问数**（预置指标语义版，不做 text2sql） | 3 |
| 5.2 | `el-dialog` → 函数式 `ReDialog`、`el-table` → `@pureadmin/table` 逐页迁移（按 `WORKBUDDY.md` §0.5：一页一改、每页浏览器复验） | 5 |
| 5.3 | 移动端响应式：教师手机审批请假 / 查看今日课表 / 快速点名 | 3 |
| 5.4 | 报表增强：目标管理与达成率、同比环比、定时导出 | 3 |
| 5.5 | 缓冲与验收 | 2 |

### Later（二期候选，未排期）

学员标签与分群、课时有效期与冻结/赠送、作业布置与提交、自定义报表、回收站与软删除、自定义角色权限、学员附件（合同/证件扫描）、家庭成员关联、转介绍关系链、渠道 ROI、考勤二维码签到。

---

## 六、关键设计决策（落地前需确认口径）

### 6.1 学员账户口径（建议值，上线前请校长确认一次）

以**实缴**为基准计算，避免欠费导致账面余额虚高：

```
实缴金额 paid          = SUM(payments.amount) - SUM(已审批通过的 refunds.amount)
已消耗金额 consumed_amt = paid × (consumed_hours / total_hours)
剩余学费 balance        = paid - consumed_amt   (= paid × remaining_hours / total_hours)
剩余课时 remaining_hours = total_hours - consumed_hours   （已有字段）
```

- **多订单**：每个订单独立计算，再在学员层面汇总；列表逐单展示，避免家长质疑时说不清
- **欠费部分**（应收 − 实缴）**不计入**剩余学费，单独在「欠费统计」体现
- **全校剩余学费总额 = 预收账款（负债）**，建议放到首页看板显眼位置——这是校长最容易误判的数字
- 现有 `/api/finance/stats/consumption` 的 `revenue_recognized = amount × consumed ÷ total_hours` 与上述口径一致，可复用

### 6.2 课堂评价维度（建议）

- **交互设计**：课次维度批量评价，全班默认「良好」，老师**只标异常学生**，大幅降低录入负担（否则必然烂尾）
- **结构化维度**（1–5 分，可选填）：专注度、参与度、作业完成、知识点掌握
- **自由文字**：一句话点评（可选，供 AI 生成报告时引用）
- **数据落点**：新建 `class_evaluations` 表（课次 + 学生 + 教师 + 维度分 + 文字 + 时间）

### 6.3 知识点体系最小集

- 表结构：`knowledge_points(id, subject, parent_id, name, sort)` 树形；`exam_scores` 关联知识点（成绩录入时按题目/题型打标签，或整卷打 1–3 个知识点）
- **只建树和标签**，不做题库、不做组卷、不做在线答题
- 历史成绩提供「补录标签」入口，否则 Phase 4 只能用新数据

### 6.4 AI 脱敏与降级

- **出网白名单**：只允许 `学号（内部 ID）+ 年级 + 结构化指标数值 + 知识点名称` 出网；**姓名、家长姓名、家长电话、金额一律不出网**
- **降级**：未配置 `LLM_API_KEY` 或调用失败 → 自动使用规则引擎模板版报告，页面明确标识「规则生成（未启用 AI）」
- **审计**：记录生成人、时间、模型名、输入指标快照，可复现

---

## 七、每阶段 Definition of Done（沿用项目强制约定）

- [ ] 数据库变更：新增迁移脚本（`server/src/migrations/0NN-*.js`，版本连续递增）并同步 `server/database.md`
- [ ] API 变更：同步 `docs/api.md`（权威）与 `docs/openapi.yaml`
- [ ] 前端：接口集中在 `src/api/*.ts`，views 内无 axios / `$route` / `localStorage`
- [ ] 浏览器验证：每个功能 Playwright 走查，截图入 `evidence/`
- [ ] 回归：`node server/scripts/e2e-lifecycle.mjs` 全绿 + `analytics-smoke.mjs` 全绿
- [ ] 构建：`pnpm lint:eslint` 0 error + `pnpm build` 退出码 0
- [ ] 部署：Docker 真机验证（`docker-verify.sh` + 生产包浏览器验证）
- [ ] 文档：`PROGRESS.md` 回填本阶段完成项与已知问题

---

## 八、风险登记

| # | 风险 | 影响 | 缓解措施 |
| --- | --- | --- | --- |
| R1 | **课评成为负担 → 老师不填 → AI 无燃料** | AI 学情分析失效 | 批量 + 只标异常；与点名提交流程绑定（提交点名后顺手评）；每周统计填写率 |
| R2 | AI 生成内容不可控 / 出现错误结论 | 误导教学决策 | 数字全部本地算；模板约束输出；教师可编辑后再发布；页面标注「AI 生成」 |
| R3 | 脱敏不到位导致学生信息外泄 | 合规风险（未成年人个人信息） | 字段白名单硬编码在 provider 层；出网前统一过滤；审计留痕 |
| R4 | 上线首周出故障 | 影响正常教学 | Phase 0 备份恢复真机演练；保留热修窗口；准备回滚方案 |
| R5 | 范围蔓延（知识库 / 题库 / 提成 / 家长端） | 工期失控 | §二 Non-goals 写死；任何新增需求先判断是否推翻 §〇 决策 |
| R6 | 单校区假设被推翻（开分店） | 架构返工 | 明确不为多校区预留；但保持 schema 可迁移（所有表已有单一 id 主键） |
| R7 | 剩余学费口径争议 | 钱算错，家长投诉 | §6.1 口径写死在文档；上线前让校长确认一次；UI 上展示计算式 |
| R8 | 历史成绩无知识点标签 | 学情分析初期数据稀疏 | 提供补录入口；Phase 4 允许「数据不足」的降级展示，不硬凑结论 |
